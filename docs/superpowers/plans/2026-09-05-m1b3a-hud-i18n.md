# M1-B3a UI 基礎與 HUD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓畫面上出現看得懂的中文 HUD——金幣、生命、關卡名、暫停鈕、速度鈕——並修掉 HUD 一存在就會讓暫停鍵靜默失效的撞鍵問題。

**Architecture:** 新增 `ui/` 層，它可以讀 `WorldState` 與 `BattleSim` 來顯示，但要讓事情發生只能發 signal，由 `battle_scene` 翻成 `InputAction` 餵給 B2 已有的 `InteractionController`。暫停按鈕與空白鍵因此走同一條、已經有測試守著的路。文字走 Godot 內建翻譯（CSV → `.translation`），中文字型是必要前提而非裝飾。

**Tech Stack:** Godot 4.7.2、GDScript、GdUnit4 6.2.1。

## Global Constraints

出自 `CLAUDE.md`、`docs/superpowers/specs/2026-09-04-tower-defense-architecture-design.md` 與 B3a 設計規格。**每個任務的要求都隱含包含本節。**

- 只使用 Godot 4.x API。禁用 `KinematicBody2D`、`yield`、`export var`。
- 只使用 GDScript，不引入 C#。
- `core/` 不得 import 任何 Node 或引擎場景類別，也不得讀檔。**B3a 完全不動 `core/`。**
- `input/` 不含任何 Node。由 `tests/test_core_purity.gd` 自動驗證。
- **`ui/` 可以讀 `WorldState` 與 `BattleSim`，但不得呼叫 `core/` 的系統、不得寫入 `WorldState`、不得自己排隊意圖。** 要讓事情發生只能發 signal。
- 傷害一律經過 `DamageSystem.apply()`。
- 實體之間只用字串 id 或整數實體 id 互相引用。
- 邏輯 tick 固定 30Hz。
- 遊戲資料一律 JSON，置於 `data/`。**翻譯放 `i18n/`，不是 `data/`**——它是資產不是遊戲資料，理由見規格 §3.2。
- **UI 與資料中只存 i18n key，不存字面文字。**
- **禁止用字串串接組出顯示文字**，格式字串本身要進翻譯檔（規格 §3.3）。
- `.gd` 檔一律用 **tab** 縮排。
- Godot 為每個 `.gd` 產生 `.uid` 檔，**必須與腳本一起提交**。

### 工作目錄

**本計畫在 worktree 中執行**：`c:/Users/yinya/git/guardians_of_formosa/.worktrees/m1b3a`，分支 `feat/m1b3a-hud`。主目錄 `c:/Users/yinya/git/guardians_of_formosa` 留給美術軌，**不要在那裡工作，也不要切換分支**。

**不要開啟 Godot 編輯器 GUI。** 全部走 headless CLI。

### 測試指令

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

`--ignoreHeadlessMode` 為必要旗標，缺少時 GdUnit4 直接 exit 103 不執行任何測試。加 `-c` 可關閉「同一檔案遇到第一個失敗就停」的行為，RED 階段要確認多條測試同時失敗時很有用。

新增檔案或資產後執行 `godot --headless --path . --import`。該指令即使成功也可能回傳非零結束碼，以輸出內容判斷。**資產變動後沒有先 `--import` 就跑測試，可能完全沒有輸出。**

既有的資料錯誤測試會印出 `push_error`，那是它們在運作，不是你造成的雜訊。

`JSON.parse_string()` 一律把數字解析成 `float`，需要整數時必須明確轉換。

### 已驗證可用的斷言

只使用這些形式：`assert_int(x).is_equal(y)`、`assert_float(x).is_equal_approx(y, eps)`、`assert_bool(x).is_true()/.is_false()`、`assert_str(s).contains(...)`、`assert_str(s).is_equal(...)`、`assert_array(a).has_size(n)/.contains([...])`、`assert_int(x).is_greater(n)`、`assert_float(x).is_greater(n)`、`assert_float(x).is_between(lo, hi)`，以及鏈在最終斷言前的 `.override_failure_message("...")`。

**沒有 `is_less`**，大小比較寫成 `assert_bool(a < b).is_true()`。**沒有驗證過 `is_not_empty`**，空值檢查寫成 `assert_bool(s != "").is_true()`。

### 起始狀態

分支 `feat/m1b3a-hud` 自 `main` 的 `ea0912a` 分出，測試 **205/205**、17 個 suite。

字型已由專案擁有者放入 `game/assets/fonts/`，三個字重：

```
TaipeiSansTCBeta-Regular.ttf   20,659,344 bytes
TaipeiSansTCBeta-Bold.ttf      21,737,516 bytes
TaipeiSansTCBeta-Light.ttf     20,279,264 bytes
```

**只提交 Regular 與 Bold**，Light 對遊戲 HUD 沒有用途。已實測 Regular 載入正常（`FontFile.load_dynamic_font` 回傳 `OK`），且 `熱蘭遮西拉雅金幣生命暫停繼續倍速` 與所有拉丁字母、數字都在字型內——一個字型檔同時涵蓋兩個語系，不需要 fallback。

---

## 已確認的環境事實

這些是實測結果，不是推測，實作時可直接依賴：

- `ThemeDB.fallback_font` 是 `Open Sans SemiBold`，`has_char("熱")` 與 `has_char("遮")` 都是 `false`。**不設字型就是整片豆腐框。**
- Godot 4.7.2 的內建動作實際內容：`ui_accept` = Enter / KP Enter / **Space**；`ui_select` = **Space** / Joy(3)；`ui_cancel` = Escape。
- 內建動作的 `InputEventKey` 存的是 **`keycode`**（`physical_keycode` 為 0），而 `gof_*` 存的是 **`physical_keycode`**。判斷 Space 時兩個欄位都要看。
- `Control` 的現行屬性是 **`auto_translate_mode`**，`auto_translate` 已棄用。
- `ProjectSettings.has_setting("internationalization/locale/translations")` 為 `true`。
- `TranslationServer.get_locale()` 目前是 `zh_TW`（跟隨作業系統）。
- `_registry.levels[&"level_01"]["name_key"]` 取得得到關卡名的 i18n key。

---

## File Structure

| 檔案 | 責任 |
|---|---|
| `input/input_bindings.gd` | 修改：把 Space 從內建 UI 動作移除 |
| `i18n/strings.csv` | 新增：六個 key × 兩個語系 |
| `project.godot` | 修改：語系登記、預設字型 |
| `.gitattributes` | 修改：ttf/otf 進 LFS |
| `game/assets/fonts/*.ttf` | 提交（人工已放入） |
| `ui/battle_hud.tscn` | 新增：HUD 版面與錨點 |
| `ui/battle_hud.gd` | 新增：HUD 邏輯，只讀不寫，發 signal |
| `game/level/battle_scene.gd` | 修改：掛載 HUD、接 signal、暫停同步修正 |
| `tests/input/test_mouse_keyboard_input.gd` | 修改：Space 已移除的四條 |
| `tests/test_i18n.gd` | 新增：key 覆蓋、雙語系解析、字型字符覆蓋 |
| `tests/test_ui_layer.gd` | 新增：`ui/` 方向規則守衛、`.tscn` 載入煙霧測試 |

`core/` 一律不動——這是 B3a 是表現層里程碑的證據。

---

### Task 1: Space 從內建 UI 動作移除

**Files:**
- Modify: `input/input_bindings.gd`
- Test: `tests/input/test_mouse_keyboard_input.gd`

**Interfaces:**
- Consumes: `InputMap`
- Produces: `InputBindings.install()` 額外把 Space 從 `ui_accept` 與 `ui_select` 移除

本任務與其餘三個完全獨立，沒有任何前置依賴。

**為什麼要做：** B2 把 `gof_toggle_pause` 綁 Space，而內建的 `ui_accept` 與 `ui_select` 也綁 Space。今天場景裡沒有任何 `Control` 所以看不出問題。**Task 3 加上 HUD 之後，玩家第一次點到暫停鈕就會讓它取得焦點，此後 Space 會被 `_gui_input` 吃掉去觸發那顆按鈕，永遠到不了 `_unhandled_input`——暫停從此失效，畫面上沒有任何線索。**

- [ ] **Step 1: 寫失敗的測試**

在 `tests/input/test_mouse_keyboard_input.gd` 末端加入：

```gdscript
## 判斷一個事件是不是 Space。內建動作存 keycode、gof_* 存 physical_keycode，
## 只看一個欄位會漏。
func _is_space_event(event: InputEvent) -> bool:
	if not (event is InputEventKey):
		return false
	var key := event as InputEventKey
	return key.keycode == KEY_SPACE or key.physical_keycode == KEY_SPACE

func _action_has_space(action_name: StringName) -> bool:
	for event: InputEvent in InputMap.action_get_events(action_name):
		if _is_space_event(event):
			return true
	return false

func test_space_is_released_from_ui_accept() -> void:
	InputBindings.install()
	assert_bool(_action_has_space(&"ui_accept")).override_failure_message(
		"ui_accept 仍綁著 Space。HUD 按鈕一取得焦點就會吃掉空白鍵，暫停靜默失效。"
	).is_false()

func test_space_is_released_from_ui_select() -> void:
	InputBindings.install()
	assert_bool(_action_has_space(&"ui_select")).override_failure_message(
		"ui_select 也綁著 Space，漏掉它等於沒修"
	).is_false()

func test_enter_survives_on_ui_accept() -> void:
	# 刻意設計成有鑑別力：把 ui_accept 整個清空的實作會通過上面那條「沒有 Space」，
	# 卻會靜默打壞 HUD 的鍵盤觸發與 M4 的手把導航。
	InputBindings.install()
	var has_enter := false
	for event: InputEvent in InputMap.action_get_events(&"ui_accept"):
		if event is InputEventKey and (event as InputEventKey).keycode == KEY_ENTER:
			has_enter = true
	assert_bool(has_enter).override_failure_message(
		"Enter 必須留在 ui_accept，否則按鈕無法用鍵盤或手把觸發"
	).is_true()

func test_releasing_space_is_idempotent() -> void:
	InputBindings.install()
	var count := InputMap.action_get_events(&"ui_accept").size()
	InputBindings.install()
	assert_int(InputMap.action_get_events(&"ui_accept").size()).override_failure_message(
		"重複安裝不得改變 ui_accept 的事件數"
	).is_equal(count)
```

- [ ] **Step 2: 執行測試確認失敗**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

Expected: FAIL。`test_space_is_released_from_ui_accept` 與 `test_space_is_released_from_ui_select` 失敗（Space 還在），另外兩條通過。

`-c` 讓兩條失敗在同一次執行中都顯示出來。**確認確實是兩條失敗、兩條通過**——通過的那兩條在此刻通過是正確的，它們守的是「修正不要過度伸手」而非「修正已完成」。

- [ ] **Step 3: 實作**

在 `input/input_bindings.gd` 的 `TOWER_CHOICE_COUNT` 之後加入：

```gdscript

## 內建的 ui_accept 與 ui_select 都把 Space 綁在上面。
const BUILTIN_ACTIONS_TO_FREE_SPACE: Array[StringName] = [&"ui_accept", &"ui_select"]
```

在 `install()` 的最後一行（塔種快捷鍵的迴圈之後）加入：

```gdscript
	_release_space_from_builtin_ui()
```

並在檔案末端加入：

```gdscript
## 把 Space 從內建的 UI 動作上拆下來。
##
## HUD 一存在，玩家點過任何按鈕之後那顆按鈕就取得焦點，Space 會被 _gui_input
## 吃掉去觸發它，永遠到不了 _unhandled_input——暫停從此失效，而且沒有任何線索。
##
## 拔 Space 而保留 Enter 與 KP Enter：按鈕照樣可聚焦、可用方向鍵導航、可用 Enter
## 觸發，M4 的手把導航不受影響。改用 focus_mode = FOCUS_NONE 反而會毀掉那件事，
## 而且那是一條「每加一顆按鈕都要記得」的紀律規則，沒有測試抓得到漏掉的那一顆。
##
## 內建動作存的是 keycode（physical_keycode 為 0），而 gof_* 存的是
## physical_keycode，所以兩個欄位都要看。
static func _release_space_from_builtin_ui() -> void:
	for action_name: StringName in BUILTIN_ACTIONS_TO_FREE_SPACE:
		if not InputMap.has_action(action_name):
			continue
		# 對副本迭代：迴圈中會從 InputMap 移除事件
		for event: InputEvent in InputMap.action_get_events(action_name).duplicate():
			if event is InputEventKey and _is_space(event as InputEventKey):
				InputMap.action_erase_event(action_name, event)

static func _is_space(event: InputEventKey) -> bool:
	return event.keycode == KEY_SPACE or event.physical_keycode == KEY_SPACE
```

- [ ] **Step 4: 執行全套件確認通過**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **209** 個測試全部 PASS（205 + 4 新增），exit 0。

- [ ] **Step 5: 提交**

```bash
git add input/input_bindings.gd tests/input/test_mouse_keyboard_input.gd
git commit -m "fix(input): 把 Space 從內建 ui_accept 與 ui_select 移除"
```

---

### Task 2: i18n 管線與中文字型

**Files:**
- Create: `i18n/strings.csv`
- Modify: `project.godot`
- Modify: `.gitattributes`
- Commit: `game/assets/fonts/TaipeiSansTCBeta-Regular.ttf`、`TaipeiSansTCBeta-Bold.ttf`
- Test: `tests/test_i18n.gd`

**Interfaces:**
- Consumes: `TranslationServer`、`ProjectSettings`、`FileAccess`
- Produces: 六個可用的 i18n key、設定好的預設字型

字型與翻譯放在同一個任務，因為它們是同一件交付物——字型的存在意義就是把那些字串畫出來，缺一個另一個就沒有價值。

- [ ] **Step 1: 寫失敗的測試**

Create `tests/test_i18n.gd`:

```gdscript
extends GdUnitTestSuite

## i18n 管線的完整性守衛。
##
## 「加了 key 忘了補英文」不是可能發生，是必然發生，而且不會有任何執行期錯誤——
## 英文語系下就只是顯示出 key 本身。這種腐化只有測試抓得到。

const CSV_PATH := "res://i18n/strings.csv"
const LOCALES: Array[String] = ["zh_TW", "en"]
const DATA_ROOT := "res://data"

var _saved_locale := ""

func before() -> void:
	_saved_locale = TranslationServer.get_locale()

func after() -> void:
	# TranslationServer 是行程全域的。不還原會汙染同一個行程裡的其他測試套件——
	# 與 InputMap 是同一類全域狀態陷阱。
	TranslationServer.set_locale(_saved_locale)

## 讀 CSV，回傳 key -> {locale: 譯文}
func _read_csv() -> Dictionary:
	var rows: Dictionary = {}
	var file := FileAccess.open(CSV_PATH, FileAccess.READ)
	if file == null:
		return rows
	var header := file.get_csv_line()
	while not file.eof_reached():
		var line := file.get_csv_line()
		if line.size() < header.size() or line[0] == "":
			continue
		var by_locale: Dictionary = {}
		for i in range(1, header.size()):
			by_locale[header[i]] = line[i]
		rows[line[0]] = by_locale
	return rows

func test_the_csv_exists_and_has_both_locales() -> void:
	var file := FileAccess.open(CSV_PATH, FileAccess.READ)
	assert_bool(file != null).override_failure_message(
		"找不到 %s" % CSV_PATH
	).is_true()
	if file == null:
		return    # GdUnit 的斷言不會中止執行，後面會對 null 取值

	# get_csv_line() 回傳 PackedStringArray，轉成 Array 再交給 assert_array
	var header := Array(file.get_csv_line())
	for locale: String in LOCALES:
		assert_array(header).override_failure_message(
			"CSV 標頭缺少語系欄 %s；標頭是 %s" % [locale, header]
		).contains([locale])

func test_every_key_resolves_in_every_locale() -> void:
	# 這條同時抓兩種腐化：語系沒登記進 project.godot（tr 回傳 key 本身），
	# 以及某個語系的欄位是空的。
	var rows := _read_csv()
	assert_int(rows.size()).override_failure_message(
		"CSV 一個 key 都沒讀到，守衛形同虛設"
	).is_greater(0)

	for locale: String in LOCALES:
		TranslationServer.set_locale(locale)
		for key: String in rows.keys():
			var translated := TranslationServer.translate(key)
			assert_bool(translated != key).override_failure_message(
				"語系 %s 下 key '%s' 沒有翻譯——回傳的是 key 本身，代表語系未登記或該欄為空" % [locale, key]
			).is_true()
			assert_bool(translated != "").override_failure_message(
				"語系 %s 下 key '%s' 的譯文是空字串" % [locale, key]
			).is_true()

func test_data_name_keys_exist_in_the_csv() -> void:
	# data/ 裡的 name_key 是唯一會從 JSON 一路帶到畫面上的 i18n key。
	# 資料裡新增一個關卡卻忘了補譯文，就是在遊戲裡顯示出 "level.level_02.name"。
	var rows := _read_csv()
	var found_any := false
	for path: String in _collect_json_files(DATA_ROOT):
		var text := FileAccess.get_file_as_string(path)
		var parsed: Variant = JSON.parse_string(text)
		if not (parsed is Dictionary):
			continue
		var dict: Dictionary = parsed
		if not dict.has("name_key"):
			continue
		found_any = true
		var key: String = dict["name_key"]
		assert_bool(rows.has(key)).override_failure_message(
			"%s 的 name_key '%s' 在 %s 裡找不到" % [path, key, CSV_PATH]
		).is_true()
	assert_bool(found_any).override_failure_message(
		"在 %s 底下找不到任何帶 name_key 的 JSON，守衛形同虛設" % DATA_ROOT
	).is_true()

func test_the_default_font_covers_every_character_used() -> void:
	# Godot 預設字型不含中日韓字符，實測 has_char("熱") 為 false。
	# 沒設字型或設錯路徑，畫面上就是一排豆腐框，而那要到人工驗收才看得見。
	var font_path: String = ProjectSettings.get_setting("gui/theme/custom_font", "")
	assert_bool(font_path != "").override_failure_message(
		"未設定 gui/theme/custom_font，所有中文都會是豆腐框"
	).is_true()

	var font: Font = load(font_path)
	assert_bool(font != null).override_failure_message(
		"載入不了字型 %s" % font_path
	).is_true()

	var rows := _read_csv()
	for key: String in rows.keys():
		for locale: String in LOCALES:
			var text: String = rows[key][locale]
			for i in text.length():
				var code := text.unicode_at(i)
				assert_bool(font.has_char(code)).override_failure_message(
					"字型缺少字符 '%s'（來自 %s 的 %s 欄）" % [text[i], key, locale]
				).is_true()

func _collect_json_files(root: String) -> Array[String]:
	var found: Array[String] = []
	var dir := DirAccess.open(root)
	if dir == null:
		return found
	for file_name in dir.get_files():
		if file_name.ends_with(".json"):
			found.append(root.path_join(file_name))
	for sub_dir in dir.get_directories():
		found.append_array(_collect_json_files(root.path_join(sub_dir)))
	return found
```

- [ ] **Step 2: 執行測試確認失敗**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

Expected: FAIL，五條全掛——CSV 不存在、字型設定不存在。

- [ ] **Step 3: 建立翻譯 CSV**

Create `i18n/strings.csv`（UTF-8 無 BOM，LF 換行）:

```
keys,zh_TW,en
hud.gold_format,金幣 %d,Gold %d
hud.lives_format,生命 %d,Lives %d
hud.speed_format,%d 倍速,%dx
hud.pause,暫停,Pause
hud.resume,繼續,Resume
level.level_01.name,熱蘭遮,Zeelandia
```

格式字串本身進翻譯檔，不是把 `金幣` 和數字串接起來——語序會變，串接寫法要到換語言時才會爆，屆時每一處都要改。

- [ ] **Step 4: 設定 project.godot**

在 `[editor_plugins]` 區塊之後、`[rendering]` 之前插入兩個新區塊（Godot 自己寫檔時是照字母序排的，照著擺可減少之後的 diff 雜訊）：

```
[gui]

theme/custom_font="res://game/assets/fonts/TaipeiSansTCBeta-Regular.ttf"

[internationalization]

locale/translations=PackedStringArray("res://i18n/strings.en.translation", "res://i18n/strings.zh_TW.translation")
```

`.translation` 是 CSV 的匯入產物，**不進版控**（`.gitignore` 已有 `*.translation`），`--import` 會重建。

- [ ] **Step 5: 讓 ttf/otf 走 LFS**

在 `.gitattributes` 的音效那幾行之後加入：

```
*.ttf filter=lfs diff=lfs merge=lfs -text
*.otf filter=lfs diff=lfs merge=lfs -text
```

- [ ] **Step 6: 匯入並確認翻譯檔真的產生了**

```bash
godot --headless --path . --import
ls i18n/
```

Expected: `i18n/` 底下除了 `strings.csv` 與 `strings.csv.import` 之外，出現 `strings.zh_TW.translation` 與 `strings.en.translation`。

**若沒有出現**，代表 `.csv` 被指派成「保持原樣」而非 CSV 翻譯匯入器。此時檢查 `i18n/strings.csv.import` 的 `importer=` 欄位是否為 `csv_translation`，不是的話改成它再重新匯入。若仍無法解決，回報 BLOCKED 而不要自己換一套機制。

- [ ] **Step 7: 執行全套件確認通過**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **213** 個測試全部 PASS（209 + `tests/test_i18n.gd` 的 4 條），exit 0。

那四條是 `test_the_csv_exists_and_has_both_locales`、`test_every_key_resolves_in_every_locale`、`test_data_name_keys_exist_in_the_csv`、`test_the_default_font_covers_every_character_used`。數字對不上就是漏寫或多寫了測試函式，先查清楚再往下走。

- [ ] **Step 8: 驗證字型測試真的會咬人**

暫時把 `project.godot` 的 `theme/custom_font` 指向一個不存在的路徑，重跑套件。

Expected: `test_the_default_font_covers_every_character_used` 失敗。

還原後重跑確認回到全綠。**這條測試守的是整個里程碑最容易靜默失敗的地方**，值得花這一步證明它有效。

- [ ] **Step 9: 提交**

```bash
git add i18n/strings.csv i18n/strings.csv.import project.godot .gitattributes \
        game/assets/fonts/TaipeiSansTCBeta-Regular.ttf \
        game/assets/fonts/TaipeiSansTCBeta-Bold.ttf \
        game/assets/fonts/TaipeiSansTCBeta-Regular.ttf.import \
        game/assets/fonts/TaipeiSansTCBeta-Bold.ttf.import
git commit -m "feat(i18n): 建立翻譯管線與中文字型"
```

`git lfs ls-files` 應列出兩個 ttf。**Light 不提交**——遊戲 HUD 沒有用途。

---

### Task 3: BattleHud 場景與腳本

**Files:**
- Create: `ui/battle_hud.tscn`
- Create: `ui/battle_hud.gd`
- Test: `tests/test_ui_layer.gd`

**Interfaces:**
- Consumes: `WorldState`（讀 `gold`、`lives`）、`BattleSim`（讀 `paused`、`speed_multiplier`）
- Produces:
  - `BattleHud extends CanvasLayer`
  - signal `pause_pressed`、`speed_pressed`
  - `BattleHud.setup(p_world: WorldState, p_sim: BattleSim, level_name_key: StringName) -> void`

**這個任務不接線。** HUD 建好但還沒掛進場景，Task 4 才接。分開的理由是版面問題與接線問題的診斷方式完全不同。

- [ ] **Step 1: 寫失敗的測試**

Create `tests/test_ui_layer.gd`:

```gdscript
extends GdUnitTestSuite

## ui/ 層的方向規則守衛，以及手寫 .tscn 的煙霧測試。
##
## 方向規則是：ui/ 可以讀 WorldState 與 BattleSim 來顯示，但要讓事情發生只能發
## signal，由 battle_scene 翻成 InputAction 餵給 InteractionController。
##
## 這條規則違反起來毫不費力——`sim.paused = not sim.paused` 一行就好，而且會動。
## 但那條路上一條測試都沒有，而兩條路遲早會漂移。所以用測試盯著。

const UI_ROOT := "res://ui"
const HUD_SCENE := "res://ui/battle_hud.tscn"

## 出現這些字串即代表 ui/ 繞過了 signal，自己動手做事
const FORBIDDEN_PATTERNS := [
	"BuildSystem",
	"queue_intent(",
	"InputAction",
]

## HUD 的節點路徑。.tscn 是盲寫的，路徑打錯只會在人工驗收才發現，
## 而人工驗收是這個里程碑最貴的一步。
const REQUIRED_NODE_PATHS := [
	"Root",
	"Root/Top",
	"Root/Top/GoldLabel",
	"Root/Top/LivesLabel",
	"Root/Top/LevelNameLabel",
	"Root/Top/PauseButton",
	"Root/Top/SpeedButton",
]

func test_ui_scripts_do_not_bypass_the_signal_boundary() -> void:
	var scripts := _collect_gd_files(UI_ROOT)
	assert_int(scripts.size()).override_failure_message(
		"在 %s 底下找不到任何 .gd 檔，守衛測試形同虛設" % UI_ROOT
	).is_greater(0)

	for path: String in scripts:
		var source := FileAccess.get_file_as_string(path)
		for pattern: String in FORBIDDEN_PATTERNS:
			assert_bool(source.contains(pattern)).override_failure_message(
				"%s 含有 '%s'。ui/ 要讓事情發生只能發 signal，由 battle_scene 翻成 InputAction。\n" % [path, pattern] +
				"繞過去的話，那條路上一條測試都沒有——而 B2 已經有測試守著 signal 那條路。"
			).is_false()

func test_the_hud_scene_loads_and_has_every_expected_node() -> void:
	var packed: PackedScene = load(HUD_SCENE)
	assert_bool(packed != null).override_failure_message(
		"載入不了 %s；.tscn 是手寫的，格式錯誤只會在這裡或人工驗收現形" % HUD_SCENE
	).is_true()

	var hud := packed.instantiate()
	for node_path: String in REQUIRED_NODE_PATHS:
		assert_bool(hud.has_node(node_path)).override_failure_message(
			"HUD 缺少節點 %s。腳本的 @onready 依這些路徑取節點，路徑錯了就是執行期 null。" % node_path
		).is_true()
	hud.free()

func test_the_hud_exposes_both_signals() -> void:
	var packed: PackedScene = load(HUD_SCENE)
	var hud := packed.instantiate()
	for signal_name: String in ["pause_pressed", "speed_pressed"]:
		assert_bool(hud.has_signal(signal_name)).override_failure_message(
			"HUD 少了 signal %s，battle_scene 接不上" % signal_name
		).is_true()
	hud.free()

func _collect_gd_files(root: String) -> Array[String]:
	var found: Array[String] = []
	var dir := DirAccess.open(root)
	if dir == null:
		return found
	for file_name in dir.get_files():
		if file_name.ends_with(".gd"):
			found.append(root.path_join(file_name))
	for sub_dir in dir.get_directories():
		found.append_array(_collect_gd_files(root.path_join(sub_dir)))
	return found
```

`_collect_gd_files` 與 `tests/test_core_purity.gd` 裡的那份重複。**刻意不抽取**：兩者守的是不同層的不同規則，而共用的只有十行走訪邏輯。第三個同類測試出現時再抽（rule of three）。

- [ ] **Step 2: 執行測試確認失敗**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

Expected: FAIL，三條全掛——`ui/` 不存在、場景檔不存在。

- [ ] **Step 3: 寫 BattleHud 腳本**

Create `ui/battle_hud.gd`:

```gdscript
class_name BattleHud
extends CanvasLayer

## 戰鬥 HUD。
##
## 只讀 WorldState 與 BattleSim 來顯示；要讓事情發生只發 signal，由 battle_scene
## 翻成 InputAction 餵給 InteractionController。
##
## 這樣做的理由不是整齊，是測試覆蓋：暫停鈕與空白鍵最後產生同一個
## InputAction.simple(TOGGLE_PAUSE)，走 B2 已經有測試守著的那條路。HUD 若自己改
## sim.paused，那條路一條測試都沒有，而兩條路遲早會漂移。
##
## 此規則由 tests/test_ui_layer.gd 的原始碼掃描守住。

signal pause_pressed
signal speed_pressed

@onready var _root: MarginContainer = $Root
@onready var _gold_label: Label = $Root/Top/GoldLabel
@onready var _lives_label: Label = $Root/Top/LivesLabel
@onready var _level_name_label: Label = $Root/Top/LevelNameLabel
@onready var _pause_button: Button = $Root/Top/PauseButton
@onready var _speed_button: Button = $Root/Top/SpeedButton

var _world: WorldState = null
var _sim: BattleSim = null

## 上一次實際寫進 Label 的值。每秒 60 次的 tr() 與字串格式化全是白燒的配置，
## 而這些值大多數幀都沒變。與「戰鬥迴圈中禁止配置新物件」是同一個考量，
## 只是換到表現層。
##
## 初始值刻意取不可能出現的數，強制第一幀一定寫入。
var _shown_gold: int = -1
var _shown_lives: int = -1
var _shown_speed: int = -1
var _shown_paused: bool = true    ## sim 起始為 false，故第一幀必定不同

func _ready() -> void:
	_pause_button.pressed.connect(_on_pause_button_pressed)
	_speed_button.pressed.connect(_on_speed_button_pressed)
	_apply_safe_area()

func setup(p_world: WorldState, p_sim: BattleSim, level_name_key: StringName) -> void:
	_world = p_world
	_sim = p_sim
	_level_name_label.text = tr(level_name_key)

func _process(_delta: float) -> void:
	if _world == null or _sim == null:
		return

	if _world.gold != _shown_gold:
		_shown_gold = _world.gold
		_gold_label.text = tr("hud.gold_format") % _shown_gold

	if _world.lives != _shown_lives:
		_shown_lives = _world.lives
		_lives_label.text = tr("hud.lives_format") % _shown_lives

	if _sim.paused != _shown_paused:
		_shown_paused = _sim.paused
		_pause_button.text = tr("hud.resume") if _shown_paused else tr("hud.pause")

	var speed := int(_sim.speed_multiplier)
	if speed != _shown_speed:
		_shown_speed = speed
		_speed_button.text = tr("hud.speed_format") % speed

func _on_pause_button_pressed() -> void:
	pause_pressed.emit()

func _on_speed_button_pressed() -> void:
	speed_pressed.emit()

## 手機的瀏海與 home indicator 會直接吃掉角落的數字。
##
## 桌面上安全區等於整個螢幕，算出來的邊距是 0，所以這段在 PC 上無作用——但它必須
## 現在就寫，因為它的失敗要到 M2 實機測試才看得見，而那時候補等於重驗一次版面。
##
## 安全區是螢幕像素，版面是 1920×1080 的設計單位，所以要換算。全螢幕時視窗尺寸
## 等於螢幕尺寸；非全螢幕時安全區可能比視窗大，換算結果會是負數，故一律夾到 0。
func _apply_safe_area() -> void:
	var window := DisplayServer.window_get_size()
	if window.x <= 0 or window.y <= 0:
		return
	var safe := DisplayServer.get_display_safe_area()
	var canvas := get_viewport().get_visible_rect().size
	var scale_x := canvas.x / float(window.x)
	var scale_y := canvas.y / float(window.y)

	_root.add_theme_constant_override("margin_left", maxi(0, int(safe.position.x * scale_x)))
	_root.add_theme_constant_override("margin_top", maxi(0, int(safe.position.y * scale_y)))
	_root.add_theme_constant_override("margin_right", maxi(0, int((window.x - safe.end.x) * scale_x)))
```

- [ ] **Step 4: 寫 HUD 場景**

Create `ui/battle_hud.tscn`:

```
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://ui/battle_hud.gd" id="1_hud"]

[node name="BattleHud" type="CanvasLayer"]
script = ExtResource("1_hud")

[node name="Root" type="MarginContainer" parent="."]
anchors_preset = 10
anchor_right = 1.0
grow_horizontal = 2

[node name="Top" type="HBoxContainer" parent="Root"]
layout_mode = 2
theme_override_constants/separation = 32

[node name="GoldLabel" type="Label" parent="Root/Top"]
layout_mode = 2
theme_override_font_sizes/font_size = 40
vertical_alignment = 1

[node name="LivesLabel" type="Label" parent="Root/Top"]
layout_mode = 2
theme_override_font_sizes/font_size = 40
vertical_alignment = 1

[node name="SpacerLeft" type="Control" parent="Root/Top"]
layout_mode = 2
size_flags_horizontal = 3

[node name="LevelNameLabel" type="Label" parent="Root/Top"]
layout_mode = 2
theme_override_font_sizes/font_size = 40
vertical_alignment = 1

[node name="SpacerRight" type="Control" parent="Root/Top"]
layout_mode = 2
size_flags_horizontal = 3

[node name="PauseButton" type="Button" parent="Root/Top"]
custom_minimum_size = Vector2(120, 120)
layout_mode = 2
theme_override_font_sizes/font_size = 36

[node name="SpeedButton" type="Button" parent="Root/Top"]
custom_minimum_size = Vector2(120, 120)
layout_mode = 2
theme_override_font_sizes/font_size = 36
```

說明幾個數字，免得被當成魔術數：

- `anchors_preset = 10` 是 `PRESET_TOP_WIDE`——貼齊上緣、佔滿寬度、高度由內容決定。HUD 因此浮在戰場上方而不佔用戰場空間（規格 §4.2）。
- `size_flags_horizontal = 3` 是 `SIZE_EXPAND_FILL`，兩個 Spacer 把關卡名推到中間。
- `custom_minimum_size = Vector2(120, 120)` 出自架構規格 §6.4 的 ≥44pt。換算：44pt ≈ 7mm，手機橫向畫面高約 65mm 對應 1080 設計單位，7 / 65 × 1080 ≈ 112，取 120 留餘裕。**這個換算建立在「手機橫向高約 65mm」的假設上，該假設沒有被量過，實體尺寸留到 M2 實機驗證。**
- `vertical_alignment = 1` 是 `VERTICAL_ALIGNMENT_CENTER`，讓文字與 120 高的按鈕對齊。
- 所有 Label 與 Button **不設 `text`**。文字全部由 `setup()` 與 `_process()` 從 i18n key 填入——CLAUDE.md 規定 UI 中只存 key，不存字面文字。

- [ ] **Step 5: 匯入並確認場景載入得了**

```bash
godot --headless --path . --import
```

Godot 可能會在匯入時替 `.tscn` 補上 `uid`。**若 `git diff` 顯示檔案被改動，那是正常的，照樣提交。**

- [ ] **Step 6: 執行全套件確認通過**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **216** 個測試全部 PASS（213 + 3 新增），exit 0。

- [ ] **Step 7: 驗證分層守衛真的會咬人**

暫時在 `ui/battle_hud.gd` 裡加一行 `# InputAction`，重跑套件。

Expected: `test_ui_scripts_do_not_bypass_the_signal_boundary` 失敗。

移除後重跑確認回到全綠。

（順帶一提：這也暴露了原始碼文字檢查的已知弱點——**註解裡的字串同樣會觸發**，反過來說被註解掉的違規呼叫也一樣會被抓到。這比漏抓好，但不精確，已記在規格 §9。）

- [ ] **Step 8: 提交**

```bash
git add ui/battle_hud.gd ui/battle_hud.gd.uid ui/battle_hud.tscn \
        tests/test_ui_layer.gd tests/test_ui_layer.gd.uid
git commit -m "feat(ui): 新增 BattleHud 場景與腳本"
```

---

### Task 4: 接線、暫停同步修正與人工驗收

**Files:**
- Modify: `game/level/battle_scene.gd`

**Interfaces:**
- Consumes: `BattleHud`、`InputAction`、`InteractionController`、`DataRegistry.levels`
- Produces: 可實際操作的 HUD

本任務是 B3a 唯一走人工驗收的部分。**Step 6 的人工驗收需要有人看著螢幕，不要嘗試自己執行。**

- [ ] **Step 1: 掛載 HUD 並接上 signal**

在 `game/level/battle_scene.gd` 的 `const SLOT_SPRITE` 之後加入：

```gdscript

const BattleHudScene := preload("res://ui/battle_hud.tscn")
```

在 `var _controller := InteractionController.new()` 之後加入：

```gdscript
var _hud: BattleHud = null
```

在 `_ready()` 的 `InputBindings.install()` 之後加入：

```gdscript

	_hud = BattleHudScene.instantiate() as BattleHud
	add_child(_hud)
	_hud.setup(world, _sim, StringName(_registry.levels[&"level_01"]["name_key"]))
	_hud.pause_pressed.connect(_on_hud_pause_pressed)
	_hud.speed_pressed.connect(_on_hud_speed_pressed)
```

注意順序：`add_child` 必須在 `setup` 之前，因為 `setup` 會寫 `_level_name_label`，而 `@onready` 要等節點進場景樹才解析。

在 `_unhandled_input` 之後加入：

```gdscript
## HUD 的按鈕與鍵盤走同一條路：翻成 InputAction 餵給控制器，而不是直接改 sim。
## B2 已經有測試守著「動作 → 意圖 → 路由器」那條路；另開一條的話那條路上
## 一條測試都沒有，而兩條路遲早會漂移。
func _on_hud_pause_pressed() -> void:
	_controller.handle(InputAction.simple(InputAction.TOGGLE_PAUSE), _sim.world)

func _on_hud_speed_pressed() -> void:
	_controller.handle(InputAction.simple(InputAction.CYCLE_SPEED), _sim.world)
```

- [ ] **Step 2: 修掉暫停中的 view 同步**

`_process` 目前是：

```gdscript
	var ticks := _sim.advance(delta)
	if ticks > 0:
		_sync_views()
```

暫停時 `advance` 回傳 0，所以暫停中建造或賣出的塔，畫面要等到恢復才更新。B1 規格已記錄此事，標為 B3 處理；B3a 的暫停鈕讓暫停從「只有鍵盤玩家會碰到」變成一等操作，所以現在修。

改成：

```gdscript
	# 暫停時 advance 回傳 0，但佇列仍會被排空（建造與賣出正是玩家暫停下來規劃時
	# 要做的事）。只看 ticks 的話，暫停中蓋的塔要到恢復才出現在畫面上。
	var had_intents := not _sim.world.pending_intents.is_empty()
	var ticks := _sim.advance(delta)
	if ticks > 0 or had_intents:
		_sync_views()
```

**注意**：`had_intents` 必須在 `advance` **之前**取，因為 `advance` 會把佇列清空。

- [ ] **Step 3: 確認單元測試未受影響**

```bash
godot --headless --path . --import
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **216** 個測試全部 PASS，exit 0。本任務只動表現層，測試數不變——**數字有變就代表有東西壞了或被誤刪**。

- [ ] **Step 4: 靜態檢查**

headless 測試套件從不載入 `battle_scene.gd`，所以那裡的解析錯誤不會被任何測試抓到：

```bash
godot --headless --path . --check-only --script game/level/battle_scene.gd
godot --headless --path . --check-only --script ui/battle_hud.gd
```

Expected: 無輸出、無錯誤。

- [ ] **Step 5: headless 冒煙**

```bash
godot --headless --path . --quit-after 2000
```

Expected: exit 0，輸出中沒有 `SCRIPT ERROR`、`Invalid`、`Nil` 或 `Parse Error`。

`--quit-after` 數的是**幀數不是秒數**，而 headless 不鎖幀率，所以實際模擬時間遠短於 `N/60` 秒。畫面上不會有塔——沒有人按鍵——這是正確的。

- [ ] **Step 6: 人工驗收（需要人看著螢幕，不要自己執行）**

```bash
godot --path .
```

依序確認：

1. HUD 出現在畫面上緣，**中文正常顯示、不是豆腐框**
2. 建塔 → 金幣減少；賣塔 → 金幣回一部分；擊殺敵人 → 金幣增加；敵人走到終點 → 生命減少
3. 按暫停鈕 → 效果與空白鍵完全相同，按鈕文字在「暫停」與「繼續」之間切換
4. **點過暫停鈕之後再按空白鍵，暫停仍然有效**（Task 1 的直接驗收）
5. 速度鈕循環 1x → 2x → 4x → 1x，鈕上顯示當前倍速
6. 關卡名顯示為「熱蘭遮」
7. **暫停中建造或賣出，畫面立刻更新**（Step 2 的驗收）
8. HUD 不遮擋建塔點與路徑

- [ ] **Step 7: 提交**

```bash
git add game/level/battle_scene.gd
git commit -m "feat(game): 掛載 HUD 並修掉暫停中的 view 同步"
```

---

## B3a 完成後

交付物：畫面上有看得懂的中文 HUD，暫停與速度可以用按鈕操作且與鍵盤同一條路，i18n 管線立起來且有測試守著兩個語系的完整性。

後續：

| 項目 | 去向 |
|---|---|
| 環形建塔選單、射程圈、長按查看資訊、置換標記替換 | B3b |
| 補足三種塔（純 JSON，`chill` 與 `splash_radius` 系統都已支援） | B3b |
| 勝敗結算與波次系統 | 獨立子里程碑 |
| 敵人生成移入 tick | 同上 |
| 字型子集化（完整 CJK 20MB，只用到六句話） | M2 記憶體閘門 |
| 觸控目標的實體尺寸驗證 | M2 實機 |
| 語系切換 UI 與設定畫面 | M4 |
| 第一個模態對話框開啟時必須一併暫停模擬 | 加入對話框的那個里程碑 |
| 原始碼文字檢查會被註解影響（雙向） | 規格 §9 已記錄 |
