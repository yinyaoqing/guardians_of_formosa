# M1-B4 波次系統與勝敗結算 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把沙盒變成一局遊戲——八波敵人、可提前呼叫換銀、撐完通關並結算救到多少人。

**Architecture:** `WaveSystem` 住在 `core/`、在 tick 裡跑，靠注入 `WorldState` 的 `enemy_defs` 造敵人，不認得場景層。通關判定同樣在 `core/`，一旦成立就讓 `BattleSim.advance()` 直接回傳 0——模擬自己停住，不需要 UI 去暫停它。呼叫下一波是一個新的意圖 kind，走 B2 建立的那條已有測試的路。

**Tech Stack:** Godot 4.7.2、GDScript、GdUnit4 6.2.1。

## Global Constraints

出自 `CLAUDE.md`、架構規格與 B4 設計規格。**每個任務的要求都隱含包含本節。**

- 只使用 Godot 4.x API。禁用 `KinematicBody2D`、`yield`、`export var`。
- 只使用 GDScript，不引入 C#。
- **`core/` 不得 import 任何 Node 或引擎場景類別。** 允許 `FileAccess`、`DirAccess`、`JSON`、`Vector2`。由 `tests/test_core_purity.gd` 驗證。
- `input/` 不含任何 Node。
- **`ui/` 可以讀 `WorldState` 與 `BattleSim`，但不得呼叫 `core/` 的系統、不得寫入它們的欄位、不得自己排隊意圖。** 要讓事情發生只能發 signal。由 `tests/test_ui_layer.gd` 的原始碼掃描守衛驗證。
- 傷害一律經過 `DamageSystem.apply()`。
- 實體之間只用字串 id 或整數實體 id 互相引用。
- **戰鬥迴圈中禁止配置新物件**，投射物與特效走物件池。
- 邏輯 tick 固定 30Hz。
- 遊戲資料一律 JSON 置於 `data/`；翻譯放 `i18n/`。
- **UI 與資料中只存 i18n key，不存字面文字。**
- **禁止用字串串接組出顯示文字**，格式字串本身要進翻譯檔。
- `.gd` 檔一律用 **tab** 縮排；`.uid` 檔必須與腳本一起提交。

### 工作目錄

**本計畫在 worktree 中執行**：`c:/Users/yinya/git/guardians_of_formosa/.worktrees/m1b4`，分支 `feat/m1b4-waves`。主目錄留給美術軌，**不要在那裡工作，也不要切換分支**。

**不要開啟 Godot 編輯器 GUI。** 全部走 headless CLI。

### 測試指令

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

`--ignoreHeadlessMode` 為必要旗標，缺少時 GdUnit4 直接 exit 103 不執行任何測試。`-c` 關閉「同一檔案遇到第一個失敗就停」。

改動資料或新增 `class_name` 後執行 `godot --headless --path . --import`；它即使成功也可能回傳非零結束碼，以輸出判斷。**改了資料沒先 `--import` 就跑測試，可能完全沒有輸出。**

既有的資料錯誤測試會印出 `push_error`，那是它們在運作。

`JSON.parse_string()` 一律把數字解析成 `float`，需要整數時必須明確轉換。

### 已驗證可用的斷言

`assert_int(x).is_equal(y)`、`assert_float(x).is_equal_approx(y, eps)`、`assert_bool(x).is_true()/.is_false()`、`assert_str(s).contains(...)`、`assert_str(s).is_equal(...)`、`assert_array(a).has_size(n)/.contains([...])`、`assert_int(x).is_greater(n)`、`assert_float(x).is_greater(n)`、`assert_float(x).is_between(lo, hi)`，以及鏈在最終斷言前的 `.override_failure_message("...")`。

**沒有 `is_less`**，寫成 `assert_bool(a < b).is_true()`。**`is_not_empty` 未驗證過**，寫成 `assert_bool(s != "").is_true()`。

### 起始狀態

分支 `feat/m1b4-waves` 自 `main` 的 `8a6c549` 分出，測試 **243/243**、22 個 suite。

---

## 已確認的環境事實

實測結果，可直接依賴：

- `_tick()` 現有順序：`_apply_pending_intents` → `status_system.tick` → `MovementSystem.tick` → `_collect_leaked` → `_rebuild_grid` → `projectile_system.tick` → `_tick_towers` → `_remove_dead`。
- `_collect_leaked()` 目前是 `world.lives -= 1`，**沒有下限**。
- `DataRegistry._load_level_dirs` **只讀每個關卡目錄裡的 `meta.json`**，其他檔案不看。所以 `waves.json` 要讓它一併讀進來。
- `DataRegistry.make_enemy(enemy_id, path_id)` 是目前唯一造敵人的地方，它 `assert` 定義存在後逐欄位填。
- `battle_scene._sync_views()` 只**更新與釋放**敵人的 view，**不建立**——建立在 `_spawn_enemy()` 裡。生成搬走之後，建立 view 的責任要移進 `_sync_views`，形狀與既有的塔那一段相同。
- `tests/` 底下引用 `lives` 的檔案有三個：`tests/core/test_battle_integration.gd`、`tests/core/test_tick_order.gd`、`tests/test_data_integrity.gd`。
- `level_01/meta.json` 目前是 `starting_gold` 200、`starting_lives` 20、`sell_refund_ratio` 0.75、`available_towers` 兩種塔。
- `orc_grunt`：hp 120、armor 0.2、speed 45、bounty 6。

---

## File Structure

| 檔案 | 責任 |
|---|---|
| `core/entities/enemy_factory.gd` | 新增：唯一的敵人建構路徑 |
| `core/entities/wave_state.gd` | 新增：波次的執行期狀態 |
| `core/systems/wave_system.gd` | 新增：倒數、生成、提前呼叫 |
| `core/data/data_registry.gd` | 修改：`make_enemy` 委派、讀 `waves.json` |
| `core/sim/world_state.gd` | 修改：`enemy_defs`、`civilians_remaining`、波次欄位、`battle_finished` |
| `core/sim/battle_sim.gd` | 修改：tick 插波次與通關判定、路由新 kind、finished 時停住 |
| `core/entities/game_intent.gd` | 修改：`KIND_CALL_NEXT_WAVE` |
| `input/input_action.gd` | 修改：`CALL_NEXT_WAVE` |
| `input/input_bindings.gd` | 修改：`gof_call_wave` 綁 `N` |
| `input/mouse_keyboard_input.gd` | 修改：翻譯新按鍵 |
| `input/interaction_controller.gd` | 修改：發出新意圖 |
| `data/levels/level_01/waves.json` | 新增：八波 |
| `data/levels/level_01/meta.json` | 修改：`starting_civilians`、`star_civilian_threshold` |
| `i18n/strings.csv` | 修改：銀、平民、波次、倒數、呼叫、結算 |
| `ui/battle_hud.tscn` / `.gd` | 修改：波次進度、倒數、呼叫按鈕 |
| `ui/result_panel.tscn` / `.gd` | 新增 |
| `game/level/battle_scene.gd` | 修改：移除自行生成、建立敵人 view、掛載結算面板 |
| `tests/core/test_wave_system.gd` | 新增 |
| `tests/core/test_enemy_factory.gd` | 新增 |
| `tests/test_data_integrity.gd` | 修改：波次資料與新欄位 |

---

### Task 1: 敵人建構收攏成一份，並注入 `enemy_defs`

**Files:**
- Create: `core/entities/enemy_factory.gd`
- Modify: `core/data/data_registry.gd`、`core/sim/world_state.gd`
- Test: `tests/core/test_enemy_factory.gd`、`tests/test_core_purity.gd`

**Interfaces:**
- Produces: `EnemyFactory.from_def(def: Dictionary, enemy_id: StringName, path_id: StringName) -> Enemy`；`WorldState.enemy_defs`

`WaveSystem` 住在 `core/`、只拿得到注入 `WorldState` 的 defs，不認得 `DataRegistry`。若它自己再寫一份造敵人的邏輯，就是第二個事實來源——與 B3b 的退款同類，只是這次兩邊都在 `core/`、沒有分層守衛擋著，所以**可以真的收攏成一份**。

- [ ] **Step 1: 寫失敗的測試**

Create `tests/core/test_enemy_factory.gd`:

```gdscript
extends GdUnitTestSuite

## 造敵人的邏輯只能有一份。DataRegistry 給場景層用，WaveSystem 給 tick 用，
## 兩邊各寫一份就會漂移——而漂移的症狀是「某條路生出來的敵人少了護甲」，
## 沒有測試同時看兩邊就抓不到。

const DEF := {
	"id": "orc_grunt",
	"name_key": "enemy.orc_grunt.name",
	"hp": 120.0,
	"speed": 45.0,
	"armor": 0.2,
	"magic_resist": 0.1,
	"bounty": 6,
	"sprite": "res://game/assets/placeholder_enemy.png",
	"frame_count": 8,
}

func test_from_def_fills_every_stat() -> void:
	var enemy := EnemyFactory.from_def(DEF, &"orc_grunt", &"main")
	assert_bool(enemy.enemy_id == &"orc_grunt").is_true()
	assert_bool(enemy.path_id == &"main").is_true()
	assert_float(enemy.hp).is_equal_approx(120.0, 0.001)
	assert_float(enemy.max_hp).is_equal_approx(120.0, 0.001)
	assert_float(enemy.base_speed).is_equal_approx(45.0, 0.001)
	assert_float(enemy.base_armor).is_equal_approx(0.2, 0.001)
	assert_float(enemy.base_magic_resist).override_failure_message(
		"魔抗漏了會讓某些敵人莫名其妙變脆，而且只在特定塔種面前看得出來"
	).is_equal_approx(0.1, 0.001)
	assert_int(enemy.bounty).is_equal(6)

func test_from_def_resets_derived_stats() -> void:
	# 衍生值每 tick 由 StatusSystem 重算，但建構時就要是合理的初值，
	# 否則新生的敵人在第一個 tick 之前速度是 0。
	var enemy := EnemyFactory.from_def(DEF, &"orc_grunt", &"main")
	assert_float(enemy.speed).override_failure_message(
		"沒有呼叫 reset_derived_stats()，新生的敵人速度會是 0"
	).is_equal_approx(45.0, 0.001)
	assert_float(enemy.armor).is_equal_approx(0.2, 0.001)

func test_a_fresh_enemy_is_alive_and_at_the_start() -> void:
	var enemy := EnemyFactory.from_def(DEF, &"orc_grunt", &"main")
	assert_bool(enemy.alive).is_true()
	assert_bool(enemy.leaked).is_false()
	assert_float(enemy.distance_along).is_equal_approx(0.0, 0.001)
```

在 `tests/test_core_purity.gd` 末端加入：

```gdscript
## 造敵人的邏輯只能有一份。DataRegistry 若自己重新展開欄位，兩條路就會漂移，
## 而 headless 測試不會發現——兩邊各自都「正確」，只是不一致。
## 與本檔其他守衛同樣是原始碼文字檢查，因為沒有其他手段看得到「有沒有委派」。
func test_data_registry_delegates_enemy_construction() -> void:
	var source := FileAccess.get_file_as_string("res://core/data/data_registry.gd")
	assert_bool(source.contains("EnemyFactory.from_def")).override_failure_message(
		"DataRegistry.make_enemy 必須委派給 EnemyFactory.from_def。\n" +
		"自己展開欄位的話，它與 WaveSystem 生出來的敵人會靜默漂移。"
	).is_true()
```

- [ ] **Step 2: 執行測試確認失敗**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

Expected: FAIL，識別字 `EnemyFactory` 未定義，且純度守衛那條也紅。

- [ ] **Step 3: 寫 EnemyFactory**

Create `core/entities/enemy_factory.gd`:

```gdscript
class_name EnemyFactory
extends RefCounted

## 從敵人定義造出 Enemy。這是全專案唯一造敵人的地方。
##
## B4 之前只有 DataRegistry.make_enemy 一條路，但波次系統住在 core/、只拿得到
## 注入 WorldState 的 enemy_defs，不認得 registry。兩邊各寫一份就是第二個事實
## 來源，而這次兩邊都在 core/、沒有分層守衛擋著，所以可以真的收攏成一份。

static func from_def(def: Dictionary, enemy_id: StringName, path_id: StringName) -> Enemy:
	var enemy := Enemy.new()
	enemy.enemy_id = enemy_id
	enemy.hp = def["hp"]
	enemy.max_hp = def["hp"]
	enemy.base_speed = def["speed"]
	enemy.base_armor = def["armor"]
	enemy.base_magic_resist = def["magic_resist"]
	enemy.reset_derived_stats()
	enemy.bounty = int(def["bounty"])
	enemy.path_id = path_id
	return enemy
```

- [ ] **Step 4: 讓 DataRegistry 委派**

把 `core/data/data_registry.gd` 的 `make_enemy` 整個換成：

```gdscript
func make_enemy(enemy_id: StringName, path_id: StringName) -> Enemy:
	assert(enemies.has(enemy_id), "找不到敵人定義: %s" % enemy_id)
	return EnemyFactory.from_def(enemies[enemy_id], enemy_id, path_id)
```

- [ ] **Step 5: 注入 enemy_defs**

在 `core/sim/world_state.gd` 的 `var tower_defs` 附近加入：

```gdscript
var enemy_defs: Dictionary = {}               ## StringName -> Dictionary
```

並在 `configure_for_level` 的 `tower_defs = registry.towers` 之後加入：

```gdscript
	enemy_defs = registry.enemies
```

- [ ] **Step 6: 匯入並執行全套件**

```bash
godot --headless --path . --import
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **247** 個測試全部 PASS（243 + 4 新增），exit 0。

- [ ] **Step 7: 驗證委派守衛真的會咬人**

暫時把 `make_enemy` 改回自己逐欄位展開（不呼叫 `EnemyFactory`），重跑套件。

Expected: `test_data_registry_delegates_enemy_construction` 失敗。

還原後重跑確認全綠。

- [ ] **Step 8: 提交**

```bash
git add core/entities/enemy_factory.gd core/entities/enemy_factory.gd.uid \
        core/data/data_registry.gd core/sim/world_state.gd \
        tests/core/test_enemy_factory.gd tests/core/test_enemy_factory.gd.uid \
        tests/test_core_purity.gd
git commit -m "refactor(core): 造敵人的邏輯收攏成一份，並注入 enemy_defs"
```

---

### Task 2: `lives` 改名為 `civilians_remaining`，貨幣顯示正名為銀

**Files:**
- Modify: `core/sim/world_state.gd`、`core/sim/battle_sim.gd`、`data/levels/level_01/meta.json`、`i18n/strings.csv`、`ui/battle_hud.gd`
- Modify: `tests/core/test_battle_integration.gd`、`tests/core/test_tick_order.gd`、`tests/test_data_integrity.gd`

**Interfaces:**
- Produces: `WorldState.civilians_remaining`；JSON 的 `starting_civilians`

第一章規格 §0.2 決定三明文排除強制失敗關卡：**撐過波次即通關，星等取決於救到多少人。** 現有的 `lives`（漏一隻扣一點、歸零就輸）與那條決定牴觸，而留著這個名字會讓下一個讀到的人假設歸零即失敗。

- [ ] **Step 1: 寫失敗的測試**

在 `tests/core/test_tick_order.gd` 末端加入：

```gdscript
func test_a_leaked_enemy_costs_one_civilian() -> void:
	var world := _make_world()
	world.civilians_remaining = 3
	var enemy := _add_enemy(world, 100000.0)   # 一 tick 就走完整條路
	var sim := BattleSim.new(world)

	sim.advance(FRAME)

	assert_bool(enemy.leaked).is_true()
	assert_int(world.civilians_remaining).override_failure_message(
		"漏過去一隻敵人，就少救一個平民"
	).is_equal(2)

func test_civilians_never_go_below_zero() -> void:
	# 歸零不是失敗，只是一個都沒救到。玩家會繼續打完剩下的波次，
	# 期間漏掉的敵人不該讓計數變成負數——結算畫面會印出負的人數。
	var world := _make_world()
	world.civilians_remaining = 1
	_add_enemy(world, 100000.0)
	_add_enemy(world, 100000.0)
	_add_enemy(world, 100000.0)
	var sim := BattleSim.new(world)

	sim.advance(FRAME)

	assert_int(world.civilians_remaining).override_failure_message(
		"三隻漏過去但只剩一個平民，應該夾在 0 而不是變成 -2"
	).is_equal(0)
```

- [ ] **Step 2: 執行測試確認失敗**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

Expected: FAIL，`civilians_remaining` 這個欄位不存在。

- [ ] **Step 3: 改名並夾住下限**

`core/sim/world_state.gd`：把 `var lives: int = 20` 換成

```gdscript
## 待撤離的平民數。漏過去一隻敵人就少救一個。
##
## 歸零**不是失敗**——第一章規格 §0.2 決定三明文排除強制失敗關卡：
## 撐過波次即通關，差別在救到多少人。所以這個數字夾在 0，且不觸發任何事。
var civilians_remaining: int = 20
```

`configure_for_level` 裡 `lives = int(meta["starting_lives"])` 換成

```gdscript
	civilians_remaining = int(meta["starting_civilians"])
```

`core/sim/battle_sim.gd` 的 `_collect_leaked` 換成：

```gdscript
func _collect_leaked() -> void:
	for enemy: Enemy in world.enemies:
		if enemy.leaked and enemy.alive:
			enemy.alive = false
			world.civilians_remaining = maxi(0, world.civilians_remaining - 1)
```

`data/levels/level_01/meta.json`：`"starting_lives": 20` 改為

```json
  "starting_civilians": 20,
  "star_civilian_threshold": 14,
```

門檻 14 是暫定值（20 人中救到七成）。

- [ ] **Step 4: 改文字**

`i18n/strings.csv`：

- `hud.gold_format` 的譯文改為 `銀 %d` / `Silver %d`
- `hud.lives_format` 那一列**整列刪除**，新增 `hud.civilians_format,平民 %d,Civilians %d`

`ui/battle_hud.gd` 裡 `tr("hud.lives_format")` 改為 `tr("hud.civilians_format")`，對應的欄位與快取變數名也一併改成平民語意（`_lives_label` → `_civilians_label`、`_shown_lives` → `_shown_civilians`），並改讀 `world.civilians_remaining`。

`ui/battle_hud.tscn` 的節點 `LivesLabel` 改名為 `CiviliansLabel`。**`tests/test_ui_layer.gd` 的 `NODE_EXPECTATIONS` 也要跟著改**，否則場景測試會紅——那是它在運作。

**欄位名 `gold` 與 JSON 的 `starting_gold` 不改。** 它的語意沒有變，變的只是顯示名稱；把 `gold` 與十幾個測試檔一起改名換到的只有命名偏好。這個落差是刻意的，已寫進設計規格 §7。

- [ ] **Step 5: 修正其餘引用**

```bash
grep -rn "lives\|starting_lives" core/ game/ ui/ tests/ data/ i18n/
```

把剩下的引用全部改掉。**注意 `test_data_integrity.gd` 可能檢查 `starting_lives` 欄位存在**，要改成新欄位名，並順便加上 `star_civilian_threshold` 的檢查。

- [ ] **Step 6: 匯入並執行全套件**

```bash
godot --headless --path . --import
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **249** 個測試全部 PASS（247 + 2 新增），exit 0。

若 i18n 完整性測試紅了，通常是 `hud.lives_format` 沒清乾淨或 `hud.civilians_format` 沒補上——那正是它存在的理由。

- [ ] **Step 7: 提交**

```bash
git add -A core/ data/ i18n/ ui/ tests/
git commit -m "refactor(core): lives 改名為 civilians_remaining，貨幣顯示正名為銀"
```

---

### Task 3: WaveSystem 與波次資料

**Files:**
- Create: `core/entities/wave_state.gd`、`core/systems/wave_system.gd`、`data/levels/level_01/waves.json`
- Modify: `core/data/data_registry.gd`、`core/sim/world_state.gd`
- Test: `tests/core/test_wave_system.gd`

**Interfaces:**
- Produces:
  - `WaveState`（`next_wave_index`、`countdown`、`spawning`、`active_groups`）
  - `WaveSystem.tick(world: WorldState, delta: float) -> void`
  - `WaveSystem.call_next_wave(world: WorldState) -> int`（回傳實際發放的獎勵，0 表示被忽略）
  - `WaveSystem.all_spawned(world: WorldState) -> bool`
  - `WorldState.waves`、`call_bonus_per_second`、`wave_state`

**這個任務不接進 tick，也不接 UI。** 純邏輯先站穩，Task 4 才接通關判定、Task 5 才接意圖。

- [ ] **Step 1: 寫失敗的測試**

Create `tests/core/test_wave_system.gd`:

```gdscript
extends GdUnitTestSuite

## 波次的推進、倒數、生成節奏與提前呼叫。整組是純邏輯，所以全部測得到——
## 這是把生成從場景層搬進 core/ 最大的好處。

const FRAME := 1.0 / 30.0
const PATH_ID := &"main"

func _make_world() -> WorldState:
	var world := WorldState.new()
	var points := PackedVector2Array([
		Vector2(0, 0), Vector2(200, 0), Vector2(400, 0), Vector2(600, 0),
	])
	world.paths[PATH_ID] = PathData.new(points, 200.0)
	world.enemy_defs = {
		&"orc_grunt": {
			"id": "orc_grunt", "name_key": "enemy.orc_grunt.name",
			"hp": 120.0, "speed": 45.0, "armor": 0.2, "magic_resist": 0.0,
			"bounty": 6, "sprite": "res://game/assets/placeholder_enemy.png",
			"frame_count": 8,
		},
	}
	world.call_bonus_per_second = 2
	world.gold = 0
	return world

## 兩波：第一波等 4 秒生 3 隻（間隔 1 秒），第二波等 6 秒生 2 隻
func _two_waves() -> Array:
	return [
		{"delay": 4.0, "groups": [
			{"enemy_id": "orc_grunt", "count": 3, "interval": 1.0, "path_id": "main", "start_delay": 0.0},
		]},
		{"delay": 6.0, "groups": [
			{"enemy_id": "orc_grunt", "count": 2, "interval": 1.0, "path_id": "main", "start_delay": 0.0},
		]},
	]

func _configure(world: WorldState, waves: Array) -> void:
	world.waves = waves
	world.reset_wave_state()

## 推進 seconds 秒，一次一個 tick
func _run(world: WorldState, seconds: float) -> void:
	var ticks := int(seconds / FRAME)
	for i in ticks:
		WaveSystem.tick(world, FRAME)

func test_the_first_wave_waits_for_its_delay() -> void:
	var world := _make_world()
	_configure(world, _two_waves())

	_run(world, 3.0)
	assert_array(world.enemies).override_failure_message(
		"第一波的 delay 是開局的準備時間，時間沒到不該有敵人"
	).has_size(0)

	_run(world, 1.5)
	assert_int(world.enemies.size()).override_failure_message(
		"4 秒的 delay 過了就該開始生"
	).is_greater(0)

func test_enemies_spawn_on_the_group_interval() -> void:
	# 刻意檢查中間的時間點：一次全生出來的實作在「跑完整波」時看起來一樣。
	var world := _make_world()
	_configure(world, _two_waves())

	_run(world, 4.0 + 0.1)
	assert_array(world.enemies).override_failure_message(
		"剛開始生時只該有第一隻"
	).has_size(1)

	_run(world, 1.0)
	assert_array(world.enemies).has_size(2)

	_run(world, 1.0)
	assert_array(world.enemies).has_size(3)

	_run(world, 3.0)
	assert_array(world.enemies).override_failure_message(
		"這一波只有 3 隻，不該繼續生"
	).has_size(3)

func test_the_next_countdown_starts_after_spawning_finishes() -> void:
	# delay 的基準是「前一波生成完畢」，不是「前一波開始」，也不是「前一波打完」。
	var world := _make_world()
	_configure(world, _two_waves())

	# 4 秒倒數 + 前兩隻的間隔 2 秒 = 第三隻生出來、該波生成完畢
	_run(world, 4.0 + 2.0 + 0.1)
	assert_array(world.enemies).has_size(3)

	_run(world, 5.5)
	assert_array(world.enemies).override_failure_message(
		"第二波的 6 秒倒數要從第一波生完才起算"
	).has_size(3)

	_run(world, 1.0)
	assert_int(world.enemies.size()).is_greater(3)

func test_groups_can_be_staggered_within_a_wave() -> void:
	var world := _make_world()
	_configure(world, [
		{"delay": 0.0, "groups": [
			{"enemy_id": "orc_grunt", "count": 1, "interval": 1.0, "path_id": "main", "start_delay": 0.0},
			{"enemy_id": "orc_grunt", "count": 1, "interval": 1.0, "path_id": "main", "start_delay": 3.0},
		]},
	])

	_run(world, 0.5)
	assert_array(world.enemies).override_failure_message(
		"第二個群組的 start_delay 是 3 秒，此刻不該出現"
	).has_size(1)

	_run(world, 3.0)
	assert_array(world.enemies).has_size(2)

func test_a_spawned_enemy_starts_at_the_path_origin() -> void:
	var world := _make_world()
	_configure(world, [
		{"delay": 0.0, "groups": [
			{"enemy_id": "orc_grunt", "count": 1, "interval": 1.0, "path_id": "main", "start_delay": 0.0},
		]},
	])
	_run(world, 0.1)

	var enemy: Enemy = world.enemies[0]
	assert_float(enemy.distance_along).is_equal_approx(0.0, 0.001)
	assert_float(enemy.position.x).override_failure_message(
		"座標沒設的話 view 會先出現在原點再跳到路徑起點，看起來像瞬移"
	).is_equal_approx(0.0, 0.001)
	assert_float(enemy.speed).override_failure_message(
		"衍生值沒重算的話新生的敵人速度是 0"
	).is_equal_approx(45.0, 0.001)

func test_calling_early_pays_the_remaining_countdown() -> void:
	# 與 B3b 的退款一致性同類：顯示的與實付的漂移，是玩家會發現、測試不會的錯。
	var world := _make_world()
	_configure(world, _two_waves())

	_run(world, 1.0)          # 倒數剩約 3 秒
	var countdown_before := world.wave_state.countdown
	var gold_before := world.gold

	var paid := WaveSystem.call_next_wave(world)

	var expected := floori(countdown_before * float(world.call_bonus_per_second))
	assert_int(paid).override_failure_message(
		"回傳的獎勵必須等於剩餘秒數 %.3f × 每秒 %d 向下取整" % [countdown_before, world.call_bonus_per_second]
	).is_equal(expected)
	assert_int(world.gold - gold_before).override_failure_message(
		"實際加進世界的銀必須與回傳值一致"
	).is_equal(paid)
	assert_int(paid).override_failure_message(
		"獎勵不該是 0，否則這條測試在比較兩個零"
	).is_greater(0)

func test_calling_early_starts_the_wave_immediately() -> void:
	var world := _make_world()
	_configure(world, _two_waves())
	_run(world, 1.0)

	WaveSystem.call_next_wave(world)
	WaveSystem.tick(world, FRAME)

	assert_int(world.enemies.size()).override_failure_message(
		"呼叫之後那一波就該立刻開始生"
	).is_greater(0)

func test_calling_while_spawning_is_ignored() -> void:
	var world := _make_world()
	_configure(world, _two_waves())
	_run(world, 4.5)          # 生成中

	var gold_before := world.gold
	var paid := WaveSystem.call_next_wave(world)

	assert_int(paid).override_failure_message(
		"生成中呼叫應靜默忽略，沒有獎勵"
	).is_equal(0)
	assert_int(world.gold).is_equal(gold_before)

func test_calling_after_every_wave_is_spawned_is_ignored() -> void:
	var world := _make_world()
	_configure(world, _two_waves())
	_run(world, 30.0)

	assert_bool(WaveSystem.all_spawned(world)).is_true()
	var gold_before := world.gold
	assert_int(WaveSystem.call_next_wave(world)).is_equal(0)
	assert_int(world.gold).is_equal(gold_before)

func test_all_spawned_is_false_while_any_wave_remains() -> void:
	# 刻意在「第一波生完、第二波還沒開始」這個空檔問——一個只看
	# 「場上有沒有敵人」的實作會在這裡錯答成 true。
	var world := _make_world()
	_configure(world, _two_waves())
	_run(world, 6.5)

	assert_bool(WaveSystem.all_spawned(world)).override_failure_message(
		"第二波還沒生，不算全部生成完畢"
	).is_false()

func test_total_spawned_matches_the_data() -> void:
	var world := _make_world()
	_configure(world, _two_waves())
	_run(world, 30.0)

	assert_array(world.enemies).override_failure_message(
		"兩波合計 3 + 2 = 5 隻，多生或少生都是節奏錯誤"
	).has_size(5)
```

- [ ] **Step 2: 執行測試確認失敗**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

Expected: FAIL，識別字 `WaveSystem` 未定義。

- [ ] **Step 3: 寫 WaveState**

Create `core/entities/wave_state.gd`:

```gdscript
class_name WaveState
extends RefCounted

## 波次的執行期狀態。只有 WaveSystem 讀寫。
##
## 不攤平到 WorldState 的欄位裡，是因為這些狀態彼此只有一起看才有意義，
## 而 WorldState 已經夠長了。

## 正在倒數或正在生成的那一波。等於 waves.size() 表示全部生成完畢。
var next_wave_index: int = 0

## 距離該波開始的秒數。只在 spawning 為 false 時有意義。
var countdown: float = 0.0

var spawning: bool = false

## 生成中的群組，每筆是 {enemy_id, path_id, remaining, interval, timer}。
## 每波開始時配置一次——一波約十幾秒才發生一次，不是戰鬥迴圈的高頻配置。
var active_groups: Array[Dictionary] = []
```

- [ ] **Step 4: 寫 WaveSystem**

Create `core/systems/wave_system.gd`:

```gdscript
class_name WaveSystem
extends RefCounted

## 波次的倒數、生成與提前呼叫。
##
## 住在 core/ 並在 tick 裡跑，所以不認得場景層——敵人定義由 configure_for_level
## 注入 WorldState，與 BuildSystem 從 tower_defs 造塔是同一個形狀。
##
## B3a 曾因為生成跑在渲染 delta 上而讓暫停與倍速對它無效；搬進 tick 之後，
## 倒數與生成自動跟著暫停與倍速，不需要任何特例。

static func tick(world: WorldState, delta: float) -> void:
	var state := world.wave_state
	if state.spawning:
		_tick_spawning(world, state, delta)
		return
	if state.next_wave_index >= world.waves.size():
		return
	state.countdown = maxf(0.0, state.countdown - delta)
	if state.countdown <= 0.0:
		_start_wave(world, state)

## 提前呼叫下一波，回傳實際發放的銀。被忽略時回傳 0。
##
## 忽略的情境（生成中、已全部生成完、倒數已歸零）一律靜默——沿用 B1 起的分野：
## 時機性的失敗靜默，資料錯誤才 push_error。
static func call_next_wave(world: WorldState) -> int:
	var state := world.wave_state
	if state.spawning or state.next_wave_index >= world.waves.size():
		return 0
	if state.countdown <= 0.0:
		return 0

	var bonus := floori(state.countdown * float(world.call_bonus_per_second))
	world.gold += bonus
	state.countdown = 0.0
	_start_wave(world, state)
	return bonus

## 所有波次都已生成完畢。注意這與「場上沒有敵人」是兩件事——
## 兩波之間的空檔場上可能是空的，但還沒生完。
static func all_spawned(world: WorldState) -> bool:
	var state := world.wave_state
	return state.next_wave_index >= world.waves.size() and not state.spawning

static func _start_wave(world: WorldState, state: WaveState) -> void:
	var wave: Dictionary = world.waves[state.next_wave_index]
	state.active_groups.clear()
	for group: Dictionary in wave["groups"]:
		state.active_groups.append({
			"enemy_id": StringName(group["enemy_id"]),
			"path_id": StringName(group["path_id"]),
			"remaining": int(group["count"]),
			"interval": float(group["interval"]),
			# timer 先擺 start_delay，歸零時生一隻，再加回 interval
			"timer": float(group.get("start_delay", 0.0)),
		})
	state.spawning = true

static func _tick_spawning(world: WorldState, state: WaveState, delta: float) -> void:
	var any_left := false
	for group: Dictionary in state.active_groups:
		if int(group["remaining"]) <= 0:
			continue
		group["timer"] = float(group["timer"]) - delta
		# while 而非 if：interval 小於一個 tick 時，一個 tick 要生好幾隻
		while float(group["timer"]) <= 0.0 and int(group["remaining"]) > 0:
			_spawn_one(world, group)
			group["remaining"] = int(group["remaining"]) - 1
			group["timer"] = float(group["timer"]) + float(group["interval"])
		if int(group["remaining"]) > 0:
			any_left = true

	if any_left:
		return

	state.spawning = false
	state.active_groups.clear()
	state.next_wave_index += 1
	if state.next_wave_index < world.waves.size():
		state.countdown = float(world.waves[state.next_wave_index]["delay"])

static func _spawn_one(world: WorldState, group: Dictionary) -> void:
	var enemy_id: StringName = group["enemy_id"]
	var def: Dictionary = world.enemy_defs.get(enemy_id, {})
	if def.is_empty():
		push_error("波次引用了不存在的敵人: %s" % enemy_id)
		return
	var path_id: StringName = group["path_id"]
	var path: PathData = world.paths.get(path_id)
	if path == null:
		push_error("波次引用了不存在的路徑: %s" % path_id)
		return

	var enemy := EnemyFactory.from_def(def, enemy_id, path_id)
	# 先把座標設到路徑起點：否則 view 會先出現在原點，等第一個 tick 才跳過去。
	enemy.position = path.position_at(0.0)
	world.add_enemy(enemy)
```

- [ ] **Step 5: WorldState 持有波次狀態**

在 `core/sim/world_state.gd` 的 `var enemy_defs` 之後加入：

```gdscript
var waves: Array = []                         ## 每筆是一波的定義
var call_bonus_per_second: int = 0
var wave_state := WaveState.new()
```

並在檔案末端加入：

```gdscript
## 把波次狀態重設到第一波的倒數。configure_for_level 之後、以及測試裡
## 直接指定 waves 之後都要呼叫，否則倒數是 0、第一波會立刻開始。
func reset_wave_state() -> void:
	wave_state = WaveState.new()
	if not waves.is_empty():
		wave_state.countdown = float(waves[0]["delay"])
```

在 `configure_for_level` 末端加入：

```gdscript
	waves = meta.get("waves", [])
	call_bonus_per_second = int(meta.get("call_bonus_per_second", 0))
	reset_wave_state()
```

- [ ] **Step 6: 讓 registry 讀 waves.json**

在 `core/data/data_registry.gd` 的 `_load_level_dirs` 中，`result[StringName(meta["id"])] = meta` 之前加入：

```gdscript
		# 波次單獨一個檔：八波的陣列擺進 meta.json 會把它撐得看不完。
		var waves_path := dir_path.path_join(sub_dir).path_join("waves.json")
		assert(FileAccess.file_exists(waves_path), "關卡目錄缺少 waves.json: %s" % sub_dir)
		var waves_text := FileAccess.get_file_as_string(waves_path)
		var waves_parsed: Variant = JSON.parse_string(waves_text)
		assert(waves_parsed is Dictionary, "JSON 格式錯誤: %s" % waves_path)
		var waves_data: Dictionary = waves_parsed
		meta["waves"] = waves_data["waves"]
		meta["call_bonus_per_second"] = waves_data["call_bonus_per_second"]
```

- [ ] **Step 7: 寫波次資料**

Create `data/levels/level_01/waves.json`，八波。**內容是暫定的**——現在只有 `orc_grunt` 一種敵人，真正的配置要等敵種補齊：

```json
{
  "call_bonus_per_second": 2,
  "waves": [
    {"delay": 15.0, "groups": [{"enemy_id": "orc_grunt", "count": 4,  "interval": 1.6, "path_id": "main", "start_delay": 0.0}]},
    {"delay": 12.0, "groups": [{"enemy_id": "orc_grunt", "count": 6,  "interval": 1.4, "path_id": "main", "start_delay": 0.0}]},
    {"delay": 12.0, "groups": [{"enemy_id": "orc_grunt", "count": 8,  "interval": 1.2, "path_id": "main", "start_delay": 0.0}]},
    {"delay": 12.0, "groups": [
      {"enemy_id": "orc_grunt", "count": 6, "interval": 1.2, "path_id": "main", "start_delay": 0.0},
      {"enemy_id": "orc_grunt", "count": 4, "interval": 1.0, "path_id": "main", "start_delay": 4.0}
    ]},
    {"delay": 14.0, "groups": [{"enemy_id": "orc_grunt", "count": 10, "interval": 1.0, "path_id": "main", "start_delay": 0.0}]},
    {"delay": 12.0, "groups": [{"enemy_id": "orc_grunt", "count": 12, "interval": 0.9, "path_id": "main", "start_delay": 0.0}]},
    {"delay": 12.0, "groups": [
      {"enemy_id": "orc_grunt", "count": 8, "interval": 1.0, "path_id": "main", "start_delay": 0.0},
      {"enemy_id": "orc_grunt", "count": 8, "interval": 1.0, "path_id": "main", "start_delay": 3.0}
    ]},
    {"delay": 15.0, "groups": [{"enemy_id": "orc_grunt", "count": 16, "interval": 0.8, "path_id": "main", "start_delay": 0.0}]}
  ]
}
```

第一波 15 秒是開局的準備時間——起始 200 銀夠蓋兩座塔。

- [ ] **Step 8: 補上波次資料的完整性測試**

波次資料裡的 `enemy_id` 與 `path_id` 是字串，打錯不會有任何編譯期錯誤——只會在該波生成時噴 `push_error`，而那可能是玩到第七波才發生。

在 `tests/test_data_integrity.gd` 末端加入：

```gdscript
func test_every_wave_group_references_real_data() -> void:
	var found_any := false
	for level_id: StringName in _registry.levels:
		var meta: Dictionary = _registry.levels[level_id]

		assert_bool(meta.has("waves")).override_failure_message(
			"關卡 %s 沒有 waves——沒有波次就沒有一局" % level_id
		).is_true()
		var waves: Array = meta["waves"]
		assert_int(waves.size()).override_failure_message(
			"關卡 %s 的波次是空的" % level_id
		).is_greater(0)

		for i in waves.size():
			var wave: Dictionary = waves[i]
			var groups: Array = wave["groups"]
			assert_int(groups.size()).override_failure_message(
				"關卡 %s 第 %d 波沒有任何群組" % [level_id, i + 1]
			).is_greater(0)

			for group: Dictionary in groups:
				found_any = true
				var enemy_id := StringName(group["enemy_id"])
				assert_bool(_registry.enemies.has(enemy_id)).override_failure_message(
					"關卡 %s 第 %d 波引用了不存在的敵人 %s。打錯字要到那一波真的生成時才會噴錯。" % [level_id, i + 1, enemy_id]
				).is_true()
				assert_int(int(group["count"])).override_failure_message(
					"關卡 %s 第 %d 波的 count 必須為正" % [level_id, i + 1]
				).is_greater(0)
				assert_float(float(group["interval"])).override_failure_message(
					"關卡 %s 第 %d 波的 interval 必須為正，否則一個 tick 會把整波生完" % [level_id, i + 1]
				).is_greater(0.0)

	assert_bool(found_any).override_failure_message(
		"一個波次群組都沒掃到，守衛形同虛設"
	).is_true()

func test_the_call_bonus_rate_is_present_and_sane() -> void:
	for level_id: StringName in _registry.levels:
		var meta: Dictionary = _registry.levels[level_id]
		assert_bool(meta.has("call_bonus_per_second")).override_failure_message(
			"關卡 %s 缺少 call_bonus_per_second，提前呼叫會永遠給 0 獎勵" % level_id
		).is_true()
		assert_int(int(meta["call_bonus_per_second"])).override_failure_message(
			"關卡 %s 的提前呼叫獎勵必須為正，否則那個操作沒有意義" % level_id
		).is_greater(0)
```

**`path_id` 的檢查刻意不放在這裡**：路徑是場景裡畫的 `Path2D`，載入時才烘焙成 `PathData`，`DataRegistry` 看不到。那條要留到 §11 提的場景資料驗證手段成形時。

- [ ] **Step 9: 匯入並執行全套件**

```bash
godot --headless --path . --import
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **262** 個測試全部 PASS（249 + 11 + 2 新增），exit 0。

- [ ] **Step 10: 驗證節奏測試真的會咬人**

暫時把 `_tick_spawning` 的 `while` 迴圈改成「一次把 `remaining` 全部生完」（忽略 `interval`），重跑套件。

Expected: `test_enemies_spawn_on_the_group_interval` 失敗——**它是唯一在半途檢查數量的測試**，跑完整波再看的測試都會通過。

還原後重跑確認全綠。

- [ ] **Step 11: 提交**

```bash
git add core/entities/wave_state.gd core/entities/wave_state.gd.uid \
        core/systems/wave_system.gd core/systems/wave_system.gd.uid \
        core/sim/world_state.gd core/data/data_registry.gd \
        data/levels/level_01/waves.json \
        tests/core/test_wave_system.gd tests/core/test_wave_system.gd.uid \n        tests/test_data_integrity.gd
git commit -m "feat(core): 新增波次系統與第一關的八波資料"
```

---

### Task 4: 接進 tick，並讓通關時模擬自己停住

**Files:**
- Modify: `core/sim/battle_sim.gd`、`core/sim/world_state.gd`
- Test: `tests/core/test_tick_order.gd`

**Interfaces:**
- Produces: `WorldState.battle_finished`

- [ ] **Step 1: 寫失敗的測試**

在 `tests/core/test_tick_order.gd` 末端加入：

```gdscript
## 一波、一隻、立刻生的世界，用來驗通關判定
func _make_one_enemy_wave_world() -> WorldState:
	var world := _make_world()
	world.enemy_defs = {
		&"orc_grunt": {
			"id": "orc_grunt", "name_key": "enemy.orc_grunt.name",
			"hp": 120.0, "speed": 0.0, "armor": 0.0, "magic_resist": 0.0,
			"bounty": 6, "sprite": "res://game/assets/placeholder_enemy.png",
			"frame_count": 8,
		},
	}
	world.waves = [
		{"delay": 0.0, "groups": [
			{"enemy_id": "orc_grunt", "count": 1, "interval": 1.0, "path_id": "main", "start_delay": 0.0},
		]},
	]
	world.reset_wave_state()
	return world

func test_waves_advance_inside_the_tick() -> void:
	var world := _make_one_enemy_wave_world()
	var sim := BattleSim.new(world)

	sim.advance(FRAME)

	assert_array(world.enemies).override_failure_message(
		"波次要在 tick 裡推進；還留在場景層的話這條會是空的"
	).has_size(1)

func test_the_battle_is_not_finished_while_an_enemy_is_alive() -> void:
	var world := _make_one_enemy_wave_world()
	var sim := BattleSim.new(world)
	sim.advance(FRAME)

	assert_bool(world.battle_finished).override_failure_message(
		"全部生成完但場上還有活著的敵人，還沒通關"
	).is_false()

func test_the_battle_finishes_on_the_very_tick_the_last_enemy_dies() -> void:
	# 這條是本任務最重要的一條。一個在 _remove_dead() 之前判定的實作會通過
	# 大部分測試——多數 tick 裡最後一隻敵人早就死了。這裡刻意讓牠「這一 tick
	# 才剛被打死」：判定若排在移除死亡之前，這一 tick 就會錯答成未完成。
	var world := _make_one_enemy_wave_world()
	var sim := BattleSim.new(world)
	sim.advance(FRAME)
	var enemy: Enemy = world.enemies[0]

	# 直接把牠打死，模擬「這一 tick 傷害剛好結算完」
	enemy.hp = 0.0
	enemy.alive = false
	sim.advance(FRAME)

	assert_array(world.enemies).has_size(0)
	assert_bool(world.battle_finished).override_failure_message(
		"最後一隻在這一 tick 被移除，通關判定必須在同一 tick 成立"
	).is_true()

func test_a_finished_battle_stops_advancing() -> void:
	var world := _make_one_enemy_wave_world()
	var sim := BattleSim.new(world)
	sim.advance(FRAME)
	world.enemies[0].alive = false
	sim.advance(FRAME)
	assert_bool(world.battle_finished).is_true()

	var ticks_before := sim.tick_count
	var ticks := sim.advance(FRAME * 10.0)

	assert_int(ticks).override_failure_message(
		"通關後 advance() 必須回傳 0——模擬自己停住，不需要 UI 去暫停它"
	).is_equal(0)
	assert_int(sim.tick_count).override_failure_message(
		"通關後 tick 不該再前進"
	).is_equal(ticks_before)
```

- [ ] **Step 2: 執行測試確認失敗**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

Expected: FAIL，`battle_finished` 不存在，且波次沒有在 tick 裡推進。

- [ ] **Step 3: 加上 battle_finished**

在 `core/sim/world_state.gd` 的 `var wave_state` 之後加入：

```gdscript
## 所有波次都生成完畢，且場上沒有活著的敵人。
##
## 這一關沒有「輸」——第一章規格 §0.2 決定三排除強制失敗關卡，所以這是唯一
## 的結束條件，成立即通關，差別只在救到多少平民。
var battle_finished: bool = false
```

- [ ] **Step 4: 接進 tick**

在 `core/sim/battle_sim.gd` 的 `_tick()` 中，把 `_apply_pending_intents()` 之後那一行改成兩行：

```gdscript
	_apply_pending_intents()
	WaveSystem.tick(world, TICK_DELTA)
	world.status_system.tick(world.enemies, TICK_DELTA)
```

並在 `_remove_dead()` 之後加入：

```gdscript
	_check_battle_finished()
```

在 `_remove_dead` 之後加入：

```gdscript
## 通關判定。排在移除死亡之後——「場上沒有活著的敵人」要在死亡結算完才問得準，
## 否則最後一隻剛死的那一 tick 會錯答成未完成，而畫面上完全看不出差別。
func _check_battle_finished() -> void:
	if world.battle_finished:
		return
	if not WaveSystem.all_spawned(world):
		return
	if not world.enemies.is_empty():
		return
	world.battle_finished = true
```

在 `advance()` 的**最開頭**（`if paused:` 之前）加入：

```gdscript
	# 通關之後模擬自己停住。讓 UI 去寫 paused 會是第二條改變模擬狀態的路，
	# 而 B2 與 B3a 花了整整兩個里程碑確保只有一條。
	if world.battle_finished:
		return 0
```

- [ ] **Step 5: 匯入並執行全套件**

```bash
godot --headless --path . --import
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **266** 個測試全部 PASS（262 + 4 新增），exit 0。

**既有測試可能因為波次現在會在 tick 裡跑而受影響**——`test_tick_order.gd` 的許多世界沒有設 `waves`，`world.waves` 是空陣列、`WaveSystem.tick` 立刻返回，所以不該有影響。若有測試紅了，先確認是不是那個世界剛好設了 waves，不要急著改測試。

- [ ] **Step 6: 驗證通關判定的位置真的重要**

暫時把 `_check_battle_finished()` 從 `_remove_dead()` 之後移到之前，重跑套件。

Expected: `test_the_battle_finishes_on_the_very_tick_the_last_enemy_dies` 失敗。

還原後重跑確認全綠。

- [ ] **Step 7: 提交**

```bash
git add core/sim/battle_sim.gd core/sim/world_state.gd tests/core/test_tick_order.gd
git commit -m "feat(core): 波次接進 tick，通關時模擬自己停住"
```

---

### Task 5: 提前呼叫的意圖與輸入層

**Files:**
- Modify: `core/entities/game_intent.gd`、`core/sim/battle_sim.gd`、`input/input_action.gd`、`input/input_bindings.gd`、`input/mouse_keyboard_input.gd`、`input/interaction_controller.gd`
- Test: `tests/core/test_tick_order.gd`、`tests/input/test_interaction_controller.gd`、`tests/input/test_mouse_keyboard_input.gd`

**Interfaces:**
- Produces: `GameIntent.KIND_CALL_NEXT_WAVE` / `GameIntent.call_next_wave()`；`InputAction.CALL_NEXT_WAVE`；InputMap 動作 `gof_call_wave`（綁 `N`）

呼叫下一波走與建塔完全一樣的路。**新增的文法只有一個 kind**，B2 建立的「動作 → 意圖 → 路由器」那條路照樣覆蓋。

- [ ] **Step 1: 寫失敗的測試**

在 `tests/core/test_tick_order.gd` 末端加入（`_make_one_enemy_wave_world` 是 Task 4 在同一個檔案裡加的 helper，直接沿用）：

```gdscript
func test_a_call_next_wave_intent_starts_the_wave_and_pays() -> void:
	var world := _make_one_enemy_wave_world()
	world.waves[0]["delay"] = 5.0
	world.call_bonus_per_second = 2
	world.reset_wave_state()
	world.gold = 0
	var sim := BattleSim.new(world)

	world.queue_intent(GameIntent.call_next_wave())
	sim.advance(FRAME)

	assert_int(world.gold).override_failure_message(
		"意圖必須真的走到 WaveSystem——倒數剩 5 秒、每秒 2，應該入帳 10"
	).is_equal(10)
	assert_array(world.enemies).override_failure_message(
		"呼叫之後那一波要立刻開始生"
	).has_size(1)
```

在 `tests/input/test_interaction_controller.gd` 末端加入：

```gdscript
func test_call_next_wave_needs_no_selection() -> void:
	var world := _make_world_with_towers()
	var controller := InteractionController.new()
	controller.handle(InputAction.simple(InputAction.CALL_NEXT_WAVE), world)
	assert_array(world.pending_intents).override_failure_message(
		"呼叫下一波與選中什麼無關，未選取時也該發得出去"
	).has_size(1)
	assert_str(world.pending_intents[0].kind).is_equal("call_next_wave")
```

在 `tests/input/test_mouse_keyboard_input.gd` 末端加入：

```gdscript
func test_n_becomes_call_next_wave() -> void:
	assert_str(MouseKeyboardInput.translate(_key_event(KEY_N), WORLD_POS).kind).is_equal("call_next_wave")
```

並把 `test_install_registers_every_action` 的動作名清單加上 `"gof_call_wave"`。

- [ ] **Step 2: 執行測試確認失敗**

```bash
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

Expected: FAIL，三個檔案都紅。

- [ ] **Step 3: 意圖與路由**

`core/entities/game_intent.gd`：在 `KIND_CYCLE_SPEED` 之後加入

```gdscript
const KIND_CALL_NEXT_WAVE := &"call_next_wave"
```

並在檔案末端加入

```gdscript
static func call_next_wave() -> GameIntent:
	var intent := GameIntent.new()
	intent.kind = KIND_CALL_NEXT_WAVE
	return intent
```

`core/sim/battle_sim.gd` 的 `_apply_pending_intents` 的 `match` 中，在 `KIND_CYCLE_SPEED` 那個 case 之後加入：

```gdscript
			GameIntent.KIND_CALL_NEXT_WAVE:
				WaveSystem.call_next_wave(world)
```

- [ ] **Step 4: 輸入層**

`input/input_action.gd`：在 `CYCLE_SPEED` 之後加入

```gdscript
const CALL_NEXT_WAVE := &"call_next_wave"
```

`input/input_bindings.gd` 的 `install()` 中，在 `gof_cycle_speed` 之後加入

```gdscript
	_bind_key(&"gof_call_wave", KEY_N)
```

`input/mouse_keyboard_input.gd` 的 `translate` 中，在 `gof_cycle_speed` 那一段之後加入

```gdscript
	if event.is_action_pressed(&"gof_call_wave"):
		return InputAction.simple(InputAction.CALL_NEXT_WAVE)
```

`input/interaction_controller.gd` 的 `handle` 的 `match` 中，在 `CYCLE_SPEED` 之後加入

```gdscript
		InputAction.CALL_NEXT_WAVE:
			world.queue_intent(GameIntent.call_next_wave())
```

- [ ] **Step 5: 匯入並執行全套件**

```bash
godot --headless --path . --import
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
```

Expected: **269** 個測試全部 PASS（266 + 3 新增），exit 0。

- [ ] **Step 6: 驗證路由沒有靜默漏掉新 kind**

暫時把 `_apply_pending_intents` 裡 `KIND_CALL_NEXT_WAVE` 那個 case 刪掉，重跑套件。

Expected: `test_a_call_next_wave_intent_starts_the_wave_and_pays` 失敗，且 `BuildSystem` 會印出未知 kind 的 `push_error`——那正是路由器把未知 kind 交給 `BuildSystem` 兜底的設計在運作。

還原後重跑確認全綠。

- [ ] **Step 7: 提交**

```bash
git add core/ input/ tests/
git commit -m "feat(input): 新增提前呼叫下一波的意圖與按鍵"
```

---

### Task 6: HUD 與結算面板

**Files:**
- Modify: `ui/battle_hud.tscn`、`ui/battle_hud.gd`、`i18n/strings.csv`、`tests/test_ui_layer.gd`
- Create: `ui/result_panel.tscn`、`ui/result_panel.gd`

**Interfaces:**
- Produces:
  - `BattleHud` 新增 signal `call_wave_pressed`
  - `ResultPanel extends CanvasLayer`，signal `restart_pressed`，`show_result(stars: int, waves: int, saved: int, total: int)`

**這個任務不接線。** Task 7 才掛進場景。

- [ ] **Step 1: 加翻譯**

`i18n/strings.csv` 新增：

```
hud.wave_format,第 %d / %d 波,Wave %d / %d
hud.countdown_format,%d 秒,%ds
hud.wave_incoming,進行中,Incoming
hud.call_wave,呼叫,Call
result.title,撐過來了,You Held
result.waves_format,撐過 %d 波,Survived %d waves
result.saved_format,救到 %d / %d 人,Saved %d / %d
result.restart,再來一次,Play Again
```

- [ ] **Step 2: HUD 加三樣**

`ui/battle_hud.tscn`：在 `LevelNameLabel` 之後、`SpacerRight` 之前插入三個節點，沿用既有的字級與尺寸慣例：

```
[node name="WaveLabel" type="Label" parent="Root/Top"]
layout_mode = 2
theme_override_font_sizes/font_size = 40
vertical_alignment = 1

[node name="CountdownLabel" type="Label" parent="Root/Top"]
layout_mode = 2
theme_override_font_sizes/font_size = 40
vertical_alignment = 1

[node name="CallWaveButton" type="Button" parent="Root/Top"]
custom_minimum_size = Vector2(120, 120)
layout_mode = 2
theme_override_font_sizes/font_size = 36
```

`ui/battle_hud.gd`：加 signal、`@onready` 與快取欄位，並在 `_process` 的比對鏈末端加入

```gdscript
	var wave_index := _sim.world.wave_state.next_wave_index
	var wave_total := _sim.world.waves.size()
	if wave_index != _shown_wave_index or wave_total != _shown_wave_total:
		_shown_wave_index = wave_index
		_shown_wave_total = wave_total
		# 顯示是 1 起算：next_wave_index 是 0 起算的索引，全部生完時等於總數，
		# 那時顯示總數本身而不是總數 + 1。
		_wave_label.text = tr("hud.wave_format") % [mini(wave_index + 1, wave_total), wave_total]

	# 倒數只顯示到秒。每幀寫一次 Label 是白燒的配置，而秒數一秒才變一次。
	var countdown := 0 if _sim.world.wave_state.spawning else ceili(_sim.world.wave_state.countdown)
	if countdown != _shown_countdown:
		_shown_countdown = countdown
		_countdown_label.text = tr("hud.wave_incoming") if countdown <= 0 else tr("hud.countdown_format") % countdown
```

按鈕的 `text` 在 `_ready` 設為 `tr("hud.call_wave")`，`pressed` 連到發出 `call_wave_pressed`。

**`tests/test_ui_layer.gd` 的 `NODE_EXPECTATIONS` 要加上三個新節點與它們的 `mouse_filter` 期望**——Label 是 `IGNORE`，Button 是 `STOP`。漏掉的話那條測試會紅，那是它在運作。

- [ ] **Step 3: 結算面板**

Create `ui/result_panel.tscn`——根節點是 `CanvasLayer`，底下一個置中的 `PanelContainer`，裡面一個 `VBoxContainer` 放星等、兩行文字與一顆按鈕。節點名分別是 `Root`、`Box`、`StarsLabel`、`WavesLabel`、`SavedLabel`、`RestartButton`。

**`Root` 這個 `PanelContainer` 要設 `mouse_filter = 0`（STOP）**——它是模態的，底下的戰場不該收到點擊。這與 B3b 的建塔選單相反（那個刻意不攔），理由是模態。

Create `ui/result_panel.gd`:

```gdscript
class_name ResultPanel
extends CanvasLayer

## 通關的結算面板。
##
## 它不需要暫停任何東西——BattleSim 在 world.battle_finished 成立時就自己停了。
## 讓面板去寫 sim.paused 會是第二條改變模擬狀態的路，而 B2 與 B3a 花了兩個
## 里程碑確保只有一條。
##
## 星等依第一章規格 §4.6：★ 撐過所有波次、★★ 平民撤離數達門檻、
## ★★★ 未失去聚落建物。第三顆恆為未達成——聚落建物還不存在。

signal restart_pressed

const STAR_FILLED := "★"
const STAR_EMPTY := "☆"
const STAR_COUNT := 3

@onready var _stars_label: Label = $Root/Box/StarsLabel
@onready var _waves_label: Label = $Root/Box/WavesLabel
@onready var _saved_label: Label = $Root/Box/SavedLabel
@onready var _restart_button: Button = $Root/Box/RestartButton

func _ready() -> void:
	visible = false
	_restart_button.text = tr("result.restart")
	_restart_button.pressed.connect(func() -> void: restart_pressed.emit())

func show_result(stars: int, waves: int, saved: int, total: int) -> void:
	# 星星是符號不是文字，兩個語系都一樣，所以不進翻譯檔——
	# 進了反而會觸發 test_the_two_locales_actually_differ。
	var filled := clampi(stars, 0, STAR_COUNT)
	_stars_label.text = STAR_FILLED.repeat(filled) + STAR_EMPTY.repeat(STAR_COUNT - filled)
	_waves_label.text = tr("result.waves_format") % waves
	_saved_label.text = tr("result.saved_format") % [saved, total]
	visible = true
```

- [ ] **Step 4: 匯入並執行全套件**

```bash
godot --headless --path . --import
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
godot --headless --path . --check-only --script ui/result_panel.gd
godot --headless --path . --check-only --script ui/battle_hud.gd
```

Expected: **269** 個測試全部 PASS（本任務不加測試），`--check-only` 無輸出。

若 i18n 完整性測試紅了，通常是某個 `tr("...")` 的 key 沒進 CSV——那正是它存在的理由。

- [ ] **Step 5: 提交**

```bash
git add ui/ i18n/strings.csv tests/test_ui_layer.gd
git commit -m "feat(ui): HUD 加上波次資訊，新增結算面板"
```

---

### Task 7: 接線、移除場景自行生成與人工驗收

**Files:**
- Modify: `game/level/battle_scene.gd`

本任務是 B4 唯一走人工驗收的部分。**Step 5 的人工驗收需要有人看著螢幕，不要嘗試自己執行。**

- [ ] **Step 1: 移除場景層的生成，改由 `_sync_views` 建立敵人 view**

刪除 `const SPAWN_INTERVAL`、`var _spawn_timer`、整個 `_spawn_enemy()` 函式，以及 `_process` 裡那整段生怪計時器（含註解）。`_process` 因此變成：

```gdscript
func _process(delta: float) -> void:
	# 暫停時 advance 回傳 0，但佇列仍會被排空（建造與賣出正是玩家暫停下來規劃時
	# 要做的事）。只看 ticks 的話，暫停中蓋的塔要到恢復才出現在畫面上。
	var had_intents := not _sim.world.pending_intents.is_empty()
	var ticks := _sim.advance(delta)

	if ticks > 0 or had_intents:
		_sync_views()
	_interpolate_views()
	_update_slot_views()
	_update_result_panel()
```

`_sync_views()` 的敵人那一段改成**先建立缺少的 view，再更新**，形狀與既有的塔那一段相同：

```gdscript
	for enemy: Enemy in _sim.world.enemies:
		var view: EnemyView = _enemy_views.get(enemy.id)
		if view == null:
			# 生成搬進 tick 之後，建立 view 的責任跟著移到這裡。
			# 先用敵人當下的座標建，才不會從畫面原點滑進來。
			view = EnemyViewScript.new() as EnemyView
			view.setup(enemy.id, _registry.enemies[enemy.enemy_id]["sprite"], enemy.position)
			_view_root.add_child(view)
			_enemy_views[enemy.id] = view
		else:
			view.on_tick(enemy.position)
```

- [ ] **Step 2: 接上 HUD 的呼叫按鈕**

在 `_hud.speed_pressed.connect(...)` 之後加入：

```gdscript
	_hud.call_wave_pressed.connect(_on_hud_call_wave_pressed)
```

在 `_on_hud_speed_pressed` 之後加入：

```gdscript
func _on_hud_call_wave_pressed() -> void:
	_controller.handle(InputAction.simple(InputAction.CALL_NEXT_WAVE), _sim.world)
```

- [ ] **Step 3: 掛載結算面板**

在 `const BuildMenuScene` 之後加入：

```gdscript
const ResultPanelScene := preload("res://ui/result_panel.tscn")
```

在 `var _range_circle` 之後加入：

```gdscript
var _result_panel: ResultPanel = null
var _result_shown: bool = false
```

在 `_ready()` 的 `_view_root.add_child(_range_circle)` 之後加入：

```gdscript

	_result_panel = ResultPanelScene.instantiate() as ResultPanel
	add_child(_result_panel)
	_result_panel.restart_pressed.connect(_on_restart_pressed)
```

並在 `_on_hud_call_wave_pressed` 之後加入：

```gdscript
## 通關時把結算面板叫出來。只叫一次——面板不是每幀重畫的東西。
##
## 星等依第一章規格 §4.6。第三顆（未失去聚落建物）恆為未達成，因為聚落建物
## 還不存在；面板照樣畫出它的位置，之後補上時不必改版面。
func _update_result_panel() -> void:
	if _result_shown or not _sim.world.battle_finished:
		return
	_result_shown = true

	var meta: Dictionary = _registry.levels[LEVEL_ID]
	var total := int(meta["starting_civilians"])
	var saved := _sim.world.civilians_remaining
	var stars := 1
	if saved >= int(meta["star_civilian_threshold"]):
		stars = 2
	_result_panel.show_result(stars, _sim.world.waves.size(), saved, total)

func _on_restart_pressed() -> void:
	get_tree().reload_current_scene()
```

- [ ] **Step 4: 執行全套件、靜態檢查與冒煙**

```bash
godot --headless --path . --import
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests
godot --headless --path . --check-only --script game/level/battle_scene.gd
godot --headless --path . --quit-after 3000
```

Expected: **269** 個測試全部 PASS，測試數不變（本任務只動表現層）。`--check-only` 無輸出。冒煙 exit 0，輸出中沒有 `SCRIPT ERROR`、`Invalid`、`Nil`、`Parse Error`。

冒煙時第一波要 15 秒才開始，而 headless 不鎖幀率、`--quit-after` 數的是幀數，所以**畫面上很可能一隻敵人都還沒生出來**——那是正確的，不是失敗。

- [ ] **Step 5: 人工驗收（需要人看著螢幕，不要自己執行）**

```bash
godot --path .
```

1. 開局有準備時間，HUD 顯示「第 1 / 8 波」與倒數秒數
2. 倒數歸零 → 敵人依間隔一隻隻出來，倒數欄位改顯示「進行中」
3. 倒數中按呼叫鈕（或 `N`）→ 該波立刻開始，**銀增加的量與按下時的剩餘秒數相符**
4. 生成中按呼叫 → 沒有反應，銀不變
5. 一波生成完 → 倒數重新開始，波次進度前進到下一波
6. 敵人走到終點 → **平民數減少**（HUD 顯示「平民」不是「生命」）
7. HUD 的資源顯示是「銀」不是「金幣」
8. 撐過第 8 波且場上清空 → **結算面板出現，且敵人與投射物不再動**
9. 面板顯示星等與「救到 N / 20 人」，**第三顆星是空心的**
10. 按「再來一次」→ 整局重置，回到第 1 / 8 波，銀與平民都回到起始值
11. 暫停與倍速仍然有效，且**倒數與生成都跟著暫停與倍速**
12. 建塔選單、射程圈、快捷鍵都與 B3b 時相同

第 11 項是 B3a 那個修正的回歸驗收——生成現在住在 tick 裡，本來就該跟著。第 12 項確認這個里程碑沒有打壞上一個。

- [ ] **Step 6: 提交**

```bash
git add game/level/battle_scene.gd
git commit -m "feat(game): 移除場景層生成，接上波次 HUD 與結算面板"
```

---

## B4 完成後

交付物：一局完整的遊戲——八波敵人、可提前呼叫換銀、撐完通關並結算救到多少人、可以再玩一次。

後續：

| 項目 | 去向 |
|---|---|
| 平民作為路徑上的中立單位、聚落建物與 ★★★ | 與駐守單位一起（共用「單位攻擊單位」的機制） |
| 駐守單位（柵欄）、路徑陷阱、地面 DoT | 獨立子里程碑 |
| 新敵種與 Boss | 內容子里程碑 |
| 英雄與法術 | 各自子里程碑 |
| 潮汐與多路徑（波次資料的 `path_id` 已預留） | 第一章內容 |
| 波次的實際配置與平衡 | 等敵種補齊 |
| 主選單與關卡選擇 | 目前 `battle_scene` 就是 `run/main_scene` |
| M2 效能閘門 | 波次現在生得出量了，可以量測 |
